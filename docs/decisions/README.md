# Decision ledgers (frozen 2026-09-23)

These files are the `AGENTS.md` files as they stood at commit `35d1720`, copied verbatim on 2026-09-23 when the `AGENTS.md` set was rewritten as short operational files. They hold the decision record the code cites by number (`D123`, `Dv-3`), the phase and wave ledgers, and the rationale prose.

- The file name is the old path with `crates/` dropped and `/` replaced by `-`: `root.md` is the old root `AGENTS.md`, `ganja-core-tests.md` is the old `crates/ganja-core/tests/AGENTS.md`, `github-workflows.md` is the old `.github/workflows/AGENTS.md`.
- Facts in these files describe the tree at `35d1720`. Line numbers, counts and mechanisms in them have drifted; the code is the truth for anything current.
- These files are never edited. New decisions are appended to `ledger.md` in this directory, one entry per decision, under the rules stated at its top. `AGENTS.md` files carry no decision history.
- To find a decision: `rg 'D552' docs/decisions`.
