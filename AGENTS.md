<!-- Generated: 2026-08-04 | Updated: 2026-08-05 -->

# ganja-code

## Purpose

`ganja` is a terminal-first AI coding agent: a **behavioral port of [opencode](https://github.com/anomalyco/opencode) v1.18.13 to Rust**, with a ratatui TUI. Upstream's TypeScript is the *specification*, not source to translate — the port writes idiomatic Rust and matches observable behavior. The workspace is twelve members: the protocol, the permission engine, the teams directory and the tools and the provider wires beneath an engine that carries no terminal dependency, then a ratatui frontend, an HTTP frontend, the client that attaches to it, and the `ganja` binary that wires them together — plus `ganja-testkit`, dev-only scaffolding that no shipped binary links, and the sealed-leaf `tmux` as a twelfth member deliberately outside that dependency chain: it is neither a frontend nor beneath the engine, and nothing described here consumes it.

## Key Files

| File | Description |
|------|-------------|
| `Cargo.toml` | Workspace manifest. **Every** dependency version is declared here; member crates only opt in with `x.workspace = true`. Each entry carries a comment explaining why that crate (or that feature) is in the tree. |
| `Cargo.lock` | Resolved dependency graph; committed. Large — read only when a specific version is in question. |
| `schema/ganja-config.schema.json` | Draft 2020-12 JSON Schema for `ganja.jsonc`/`ganja.json`, so an editor's `$schema` reference gets real completion. Not upstream parity by omission — opencode publishes its own, and this is the same idea. `crates/ganja-core/tests/config_schema.rs` is the drift test: it feeds the real loader a bogus key and asserts serde's own `deny_unknown_fields` enumeration is exactly this file's `properties`, in both directions, for `Config` and the two MCP shapes, and validates a kitchen-sink document and the schema's own refusals against it. |
| `rust-toolchain.toml` | Pins the **non-dated `nightly` channel** plus clippy, rustfmt, rust-analyzer; CI's toolchain steps repeat the same channel because the action does not read this file. Edition 2024. |
| `CLAUDE.md` | Symlink to this file, so tools that look for either name read the same document. |
| `PRACTICE.md` | Phase-to-exercise mapping for the owner, a Go expert learning Rust. Explanations here lean on Go anchors. |
| `THIRD_PARTY_NOTICES.md` | Upstream MIT attribution. Any text ported from opencode (tool prompts, themes) must be recorded here — and so must anything in the tree another tool's process wrote, which since P25 means the captured Claude Code documents under `crates/ganja-team/tests/fixtures/`. |
| `dist-workspace.toml` | Local packaging only: `dist build` produces a self-contained archive (`target/distrib/`). No CI backend on purpose — publishing is deferred, so nothing may generate a workflow file. |
| `README.md` | Stub. |
| `LICENSE` | Apache-2.0. |
| `CODE_OF_CONDUCT.md` | Contributor covenant. |
| `.gitignore` | Ignores `target/`, `.omc/` (operational state) and `upstream/` (CI's spec checkout). |

## Subdirectories

| Directory | Purpose |
|-----------|---------|
| `crates/` | The workspace members (see `crates/AGENTS.md`) |
| `.github/` | CI configuration (see `.github/AGENTS.md`) |
| `docs/` | Reference documents, not specs: `references/{claude,opencode,codex}.{en,ja}.md` catalogue Claude Code's, upstream opencode's and OpenAI Codex CLI's feature surfaces against ganja's — inventories with an explicit not-a-roadmap notice, never porting obligations. |
| `.omc/` | **Gitignored operational state**, not documented by this tree: `plans/` (the authoritative port plan), `handoffs/` (frozen per-phase contracts), `reference/opencode-v1.18.13/` (the upstream checkout the golden test drives). |
| `target/` | Cargo build output. Never read. |

## Commands

```sh
cargo build --workspace
cargo run                       # TUI; no GANJA_PROVIDER set means the built-in fake provider
cargo run -- --model anthropic/claude-sonnet-4-5 --agent plan   # also --config <file>
cargo run -- --continue         # or --session <id>; the two are mutually exclusive
cargo run -- --yolo             # answer permission dialogs "allow once" automatically; hidden alias of
                                #   --auto (and --dangerously-skip-permissions) — deny rules still deny (D479)
cargo run -- auth login         # also: auth list, auth logout
cargo run -- sessions           # this project's stored conversations, roots only
cargo run -- sessions --live    # the other listing (D505): every session answering on its
                                #   socket right now. Health decides live; the flock'd `.lock`
                                #   beside a socket decides dead — a `.sock` whose lock is held
                                #   is left alone, and a `.lock` file is never removed
cargo run -- models anthropic --refresh        # the catalog, narrowed to a provider and re-fetched first
cargo run -- models cursor      # cursor's live roster from the wire itself — uncataloged, so no sizing or pricing
cargo run -- mcp                # the configured MCP servers, dialled, with the tools they lend
cargo run -- mcp login <server> # OAuth login for a remote server configured with `oauth: {}`
cargo run -- mcp add local -- npx some-server   # also: add remote --url https://…, get <name>,
                                #   remove <name>; --global targets the config home file; a
                                #   ganja.jsonc target is edited comment-preservingly (D483)
cargo run -- plugin list        # the installed plugins and what each contributes; also:
                                #   marketplace add <git-url|path> / list / remove <name> /
                                #   update [<name>] (re-fetch from the recorded origin; all when
                                #   unnamed), install <plugin>@<marketplace>, enable, disable,
                                #   remove — the `/plugin` dialog's own actions
cargo run -- skills             # the discovered skill roster — what a composer $name token invokes (D491)
cargo run -- run "what does this crate do"        # one headless turn; --format json for a script
cargo run -- run --continue --auto "now fix it"   # --auto allows what a headless run otherwise refuses
cargo run -- config import-opencode --dry-run  # translate an opencode config, naming what it skipped
cargo run -- serve --port 4096  # the engine over HTTP + SSE; loopback unless GANJA_SERVER_PASSWORD is set

# The gates CI runs, in order
cargo fmt --all --check
cargo clippy --all-targets -- -D warnings
RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps   # intra-doc links resolve
cargo nextest run --workspace       # the suite; each test in its own process
cargo test --workspace --doc        # nextest skips doctests, so run them beside it
! cargo tree -p ganja-core -e normal | grep -q ratatui   # core stays terminal-free
! cargo tree -p ganja-core -e normal | grep -q axum      # the engine never grows an HTTP server
! cargo tree -p ganja-provider -e normal | grep -q ratatui    # and neither does a wire —
! cargo tree -p ganja-provider -e normal | grep -q crossterm  #   no terminal, and no
! cargo tree -p ganja-provider -e normal | grep -q arboard    #   clipboard either
! cargo tree -p ganja-tui -e normal | grep -q axum        # the frontends stay in their lanes
! cargo tree -p ganja-serve -e normal | grep -q ratatui
! cargo tree -p ganja-client -e normal | grep -q axum
! cargo tree -p tmux -e normal | tail -n +2 | grep -q ganja-  # the sealed leaf consumes none of ours
# Every non-tmux workspace member's normal tree is checked not to list tmux; CI derives the list from cargo metadata.
# The internal-dependency allowlists, which fail closed where a `! grep` would not:
#   ganja-tool     = "ganja-permission "
#   ganja-team     = "ganja-protocol "
#   ganja-provider = "ganja-permission ganja-protocol ganja-tool "
#   ganja-core     = "ganja-permission ganja-protocol ganja-provider ganja-team ganja-tool "
#   ganja-client   = "ganja-protocol "
# And two external allowlists, because the inverted leaf gates see only `ganja-` names:
#   ganja-protocol, depth 1 = "serde serde_json uuid "
#   tmux, depth 1 = "futures thiserror tokio "
! cargo tree -p ganja-permission -e normal | tail -n +2 | grep -q ganja-  # the rules need nothing of ours
! cargo tree -p ganja-protocol -e normal | tail -n +2 | grep -q ganja-    # the wire types even less
# Every dependency version lives in the root manifest, asserted over crates/*/Cargo.toml
```

The inverted gates are deliberate: a plain `grep -c` exits non-zero on zero matches and would fail exactly when the boundary holds. Together they assert every direction that matters — the engine reaches no terminal, nothing beneath the engine reaches the engine, and the bottom crates stay leaves.

Single tests (nextest filters by name substring; `cargo test` still works and is what runs doctests):

```sh
cargo nextest run -E 'binary(golden)'                # one integration binary
cargo nextest run -E 'binary(mcp)'                   # shares golden's bun + upstream-checkout prerequisites
cargo nextest run -E 'binary(lsp)'                   # needs rust-analyzer on PATH (rustup component); hard-fails without it
cargo nextest run -p ganja-permission                # the permission engine's own tests
cargo nextest run -p ganja-provider                  # the wires, the logins and the catalog
cargo nextest run --workspace permission             # every test whose name matches "permission"
cargo nextest run -p ganja-tui snapshot_tool_error   # one snapshot test
cargo insta review                                   # TUI snapshots: crates/ganja-tui/src/snapshots/
```

Two suites need setup, and both are documented in `crates/ganja-core/tests/AGENTS.md`:

- **Golden differential** runs in the default test run (`cargo nextest run`, or `cargo test`) and **hard-fails rather than skips** when its prerequisites are missing — a green run that compared against nothing would be worthless. It needs `bun` on `PATH` and an upstream checkout with `bun install` already run, at `.omc/reference/opencode-v1.18.13` or wherever `GANJA_OPENCODE_DIR` points (CI checks it out to `upstream/`). The MCP suite (`tests/mcp.rs`) shares those prerequisites — its reference server runs on the checkout's installed `@modelcontextprotocol/sdk` — and hard-fails the same way.
- **Live provider tests** are `#[ignore]`d *and* inert unless opted in: `GANJA_LIVE_TEST=1 ANTHROPIC_API_KEY=… cargo test -p ganja-core --test live -- --ignored`.

## Environment

| Variable | Meaning |
|---|---|
| `GANJA_PROVIDER` | `anthropic` \| `openai` \| `openrouter` \| `opencode` \| `opencode-go` \| `grok` \| `github-copilot` \| `fake` \| `cursor`. Unset means `fake` with a notice in the status bar. `openrouter` speaks the Responses API at the vendor's own base on an `OPENROUTER_API_KEY`, cataloged but pinned to no default — a 349-row gateway has no obvious one, so a session names its model or is refused with the three ways to; `/effort` offers the vendor's four documented levels (sent as `reasoning.effort`), and the curated `openrouter.server_tools` config list opts a session into that vendor's server-run tools (D489). `opencode` (Zen) and `opencode-go` are the zen gateway's two tiers on one `OPENCODE_API_KEY`: the wire is picked **per model** from the catalog row's npm hint — chat-completions, Responses, or Anthropic Messages (whose `x-api-key` the gateway requires) — and a `@ai-sdk/google` row is refused by name, exactly as the vendor's own client refuses it (**D488**). `grok`, `github-copilot` and `cursor` are entered by `ganja auth login`, not by a key variable; `openai` speaks the **Responses API on either credential** — an API key against `api.openai.com/v1`, a stored ChatGPT login against the codex backend — because that vendor's wire is the vendor's, not the credential's. `cursor` speaks Connect to cursor's agent backend and still rides the uncataloged tier — no sizing, pricing or auto-compaction — but it defaults to `default`, the server-side Auto id its own wire publishes, so a session runs unnamed; the full roster is one `ganja models cursor` away, and the `/model` chooser serves the same listing. Its login pairs a browser with a long-polling terminal, and a turn carries only the newest message until history composition on cursor's blob channel lands. |
| `GANJA_MODEL` | Overrides the catalog's default model for the selected provider. A config's `provider` table adds ids of its own to what `GANJA_PROVIDER` accepts: an entry names a `dialect` (`openai-chat-completions` or `anthropic-messages`), a `base_url`, optionally the `key_env` holding its key and the `headers` it wants. Such a provider is *selectable* but not *cataloged* — sizing, pricing and auto-compaction are off for it, and it must be told which model to ask for. |
| `GANJA_FAKE_SCRIPT` | Path to a JSON script the fake provider plays (text + tool calls per turn). How PTY tests and demos drive a deterministic agent. Read only on the `from_env` route, never by `FakeProvider::default()`. |
| `GANJA_CONFIG` | An extra config file merged between the global tier and the project files. Naming a file that does not exist is an error, where an absent discovered file is nothing to merge. |
| `GANJA_CONFIG_HOME` | The directory ganja keeps its own things in — the global `ganja.jsonc`/`ganja.json`, the global `AGENTS.md`, and the `skills/` and `themes/` beneath it. Set, it outranks both discovered locations unconditionally; unset, the home is `$XDG_CONFIG_HOME/ganja` when that directory exists, else `~/.ganja` when that one does, else `$XDG_CONFIG_HOME/ganja` as the place a writer should create. One home, not a merge — somebody holding both directories is served the XDG one. Distinct from `GANJA_CONFIG`, which names a config **file** to merge in and moves nothing else. |
| `ANTHROPIC_API_KEY` / `OPENAI_API_KEY` | Credential; outranks the stored `auth.json` key. |
| `OPENROUTER_API_KEY` / `OPENCODE_API_KEY` | The same precedence for the gateways: openrouter's key, and the one key both zen tiers share — stored per id, but either id answers to the one exported variable. |
| `EDITOR` | What `/editor` hands the prompt buffer to. Unset or empty falls back to `vi`. |
| `ANTHROPIC_BASE_URL` / `OPENAI_BASE_URL` | Endpoint override; must be `https` or loopback or the provider refuses. `OPENAI_BASE_URL` now points a *Responses* client at what it names, so a chat-completions-only server (a local llama.cpp) is no longer reachable as `GANJA_PROVIDER=openai`. |
| `GANJA_MODELS_URL` | Base URL the live model catalog is fetched from (`/api.json` appended); default `https://models.opencode.ai`. |
| `GANJA_MODELS_PATH` | Read-only override of the catalog cache path; writes keep going to the canonical cache file. |
| `GANJA_DISABLE_MODELS_FETCH` | Truthy (`1`/`true`) disables all catalog fetching — the disk cache and the compiled-in snapshot still serve. |
| `GANJA_AUTH_ISSUER` | Origin every login endpoint is reached at, so a test can complete a login against endpoints it owns. **Loopback only**, matched as a whole origin — a value that is set and is not loopback is refused rather than ignored, because silently using the real issuer is the one outcome whoever set it cannot have wanted. |
| `GANJA_SERVER_PASSWORD` | When set, every `ganja serve` route requires HTTP Basic auth with it; when unset, only loopback binds are allowed. |
| `GANJA_SERVER_USERNAME` | The Basic username `serve` expects; default `ganja`. |
| `EXA_API_KEY` | Credential `websearch` presents to Exa, in that service's query string. Absent, a search that would have gone to Exa is refused naming this variable rather than sent unauthenticated — a divergence from upstream, which sends it anyway. |
| `PARALLEL_API_KEY` | The same for Parallel, presented as a bearer token. |
| `GANJA_WEBSEARCH_PROVIDER` | `exa` \| `parallel`. Names the service `websearch` asks. Unset, the service is the one whose key this machine holds — `exa` when both are. Upstream instead splits sessions by hashing the session id, which a tool here cannot see. |
| `GANJA_LIVE_TEST`, `GANJA_OPENCODE_DIR` | Test opt-ins, above. |

## Architecture

Twelve members — ten crates that ship, plus dev-only `ganja-testkit` (the fixture doubles `crates/ganja-core/tests/*.rs` share; a `[dev-dependencies]` entry and nothing else, so no shipped binary links it), plus the sealed-leaf `tmux`, deliberately outside that dependency graph and neither consumed by nor consuming any of its members (P26 user directive, 2026-08-18). The count is `cargo metadata --no-deps`'s, not a hand tally. The load-bearing boundaries are asserted rather than trusted: nothing in the engine may reach a terminal or an HTTP server, nothing beneath the engine may reach the engine, and the two bottom crates name nothing else of ours. The graph is a DAG, not a chain — frontends sit on `ganja-core`, core on `ganja-provider`, `ganja-tool` and `ganja-team`, the first two of those on `ganja-permission`, while `ganja-protocol` is a leaf that core, the provider crate, the teams crate, the frontends and `ganja-client` consume directly and that tool and permission never touch. Every "nothing beneath may reach the engine" claim is an **allowlist** in CI rather than a `! grep ganja-core`, because a blocklist names one crate and goes quiet the day a new one appears.

**`ganja-protocol`** — the types every side of the app speaks: `Command`, `Event`, `Message`/`Part`, `ToolState`, `Usage`, and the ids that sort in creation order. Serde-derived from day one; that serialization constraint — not a trait — is what preserves the path to serving the engine over a socket and to transcripts that replay from disk. Its dependency list is `serde`, the value type a tool call's arguments arrive as, and `uuid` for the id mint (**D493**) — a list that short is the whole reason it is a crate: a frontend that renders a transcript builds nothing else. P25 put the teammate surface here too: `team.rs` (the frames two agents exchange, and the `TeamView`/`MemberView` projection a frontend renders a team from), `PartBody::Peer` — a teammate's words, drawn *and* sent as §5.3's envelope while staying outside `Part::as_text` (**D495**) — and `PermissionMode` with `Command::SetPermissionMode` (**D496**). A provider-run tool call is a third display-only part, `PartBody::ServerTool` — drawn by every frontend, sent by no wire, because a Tool part is what encoders replay as a function call the roster never advertised (D489). Reasoning arrives as two parts, and the split is load-bearing: `PartBody::Reasoning` is the opaque provider state a `store: false` turn hands back on its next request, while `PartBody::ReasoningText` is thinking a person can read — **display-only**, drawn by every frontend and sent by no wire, kept out of `Part::as_text` so it can never title a checkpoint or answer a copy command, counted as nothing by the context meter, and carried by no summary. Upstream opencode fuses the two into one part and replays it into the request (`session/message-v2.ts:362-376`); ganja splits them, which is what makes "display-only" a thing the type system can state. Its tag keeps the `reasoning` prefix that `REASONING_TAG` makes a forward-compatibility contract of, so a build too old to decode one still leaves storage's marker in its place rather than dropping the row without a word.

**`ganja-permission`** — a call becomes one or more patterns; the **last matching rule wins**, and **every** pattern must be allowed for the call to run unasked. Rules layer builtin defaults < the agent's < the config's < the answers a person stored. Shell "always" answers remember the *kind* of command via upstream's arity table. A subagent inherits the refusals and never the allows: nobody is watching its turn. `project.rs` rides along — which worktree this is decides where the answers are stored and what counts as outside it — and the crate's own docs own that the name is a small lie about that.

**`ganja-team`** — Claude Code's teams directory: the member records and the file-backed mailboxes teammates are addressed through (**D497**, P25; upstream opencode has no teams, no teammates and no second agent to address, so there is no TypeScript to port and every sentence in it is ganja's own). It is a crate because the documents are **somebody else's format** — a real `claude` process may be sharing the very directory it writes into — which makes two things interop contracts rather than implementation details: the bytes of a document, and the protocol by which a writer holds an inbox. Both are served by a `#[serde(flatten)]` passthrough over an `IndexMap`, so an unknown key survives a rewrite *in the position it arrived in* — the exact opposite of `ganja-protocol`'s declare-and-refuse posture, which is why the two cannot be one crate. Locking is `proper-lockfile`'s protocol as that library defines it: a lock **directory**, never a file, broken on mtime alone past ten seconds, no pid liveness. What it does not know is where ganja keeps its homes, what a session is, or what a permission decides — the teams root arrives as a `TeamsRoot` value the way `skill::Roots` arrives in `ganja-tool`, and its internal dependency list is exactly `ganja-protocol`, asserted closed from the crate's first commit. It is deliberately **synchronous**: a mailbox write is a sub-second read-modify-write on a small file, and whoever calls it from inside a turn wraps it in `spawn_blocking`. A frontend that merely *renders* a team needs no dependency on it at all — that is what `ganja-protocol`'s own `TeamView`/`MemberView` projection is for.

**`ganja-tool`** — `Tool` trait + `Registry::with_builtins()` (read, edit, write, glob, grep, `bash`, todowrite, webfetch, `websearch` — Exa or Parallel over one JSON-RPC POST, keys from the environment, refused politely without one — `skill`, a `SKILL.md` under one of ganja's own two homes — `<config home>/skills` and `<project root>/.ganja/skills` — or a directory `skills.paths` named, loaded into the conversation on request; **nothing foreign is discovered**, a standing ruling that leaves upstream's `~/.claude`, `~/.agents` and their walk-ups unread and one config line away, and `question`, the one tool whose whole purpose is to wait for a person), plus `task`, which the engine registers once it knows which agents this session may spawn, and the two plan doors — `plan_exit`, registered once the roster holds a build agent, and `plan_enter`, its mirror, registered once it holds a plan agent and synthesized rather than ported because upstream ships that door's description and permission vocabulary and no tool body at all (**D477**). P24 added `deferral.rs` (**D492**, no upstream counterpart — the model is Claude Code's ToolSearch): the `Deferral` value — whole servers' `mcp__*` names advertise-filtered largest-first past the config's `tool_defer_threshold`, activated names exempt before the arithmetic, one order-preserving `retain_advertised` filter, and the per-step `<deferred_tools>` listing — plus the resident `tool_search`, which fetches a deferred schema by batch `select:` name or nucleo keyword and activates what it returns for the session; the registry itself stays complete, and a direct call to a deferred tool executes and activates by executing. The roster's `skill` holds no directories at all — where ganja keeps its things is not a question a tool may answer — so each of the three real frontends installs one over it holding `instruction::skill_roots`, the same value its prompt's `<available_skills>` block was composed from; a registry a *fixture* builds stays rootless, which is what keeps the golden differential comparing two agents rather than two laptops. Schemas generated from the argument structs; `FileTimes` enforces read-before-write; glob/grep run in-process on the ripgrep crates. `write` and `edit` reach the disk through a directory descriptor (`anchor.rs`), never twice through a path, so a link swapped in after the permission dialog has nothing to redirect. `watch.rs` is here too, beside the log it reports into. P25 added two more: `send_message` (**D498**), the one teammate-to-teammate door, registered by the engine only where a team exists — its sender is bound *inside* the `Postbox` in its `ToolCtx` rather than taken as an argument, so nobody can send as somebody else — and which runs **unasked**, because sending words is conversation and whatever the recipient does with them is gated by the recipient's own rules; and `socket.rs` (**D505**), which is not a tool at all but the one spelling of **what a session socket is** — `/tmp/ganja-<uid>/<8+ hex>.sock`, the `0700`-directory predicate, and the `vet_address` clauses a `uds:` address is refused by name against. Four readers at four heights need that answer (the tool at §5.2's rung 3, the engine's deliver arm, `ganja-serve`'s binder, `ganja sessions --live`) and this crate is the lowest of the four, so it is written here once and re-exported upward. **Nothing that binds moved**: the name walk, the liveness lock and the peer-uid check on accept stay in `ganja-serve/src/socket.rs`, and the allowlist is unchanged at `ganja-permission `. P27 hoisted one more answer down here, `prepare_directory` — the `0700`-directory predicate made real — because the shim orphan records live in that same `/tmp/ganja-<uid>/` and a second implementation of "is this directory ours" is a second thing to get wrong; that **amends D505's scheme/binder split** (**D509**) so the scheme crate now makes the directory and the binder keeps everything about a name. It depends on `ganja-permission` and on nothing else of ours — the engine is deliberately outside its graph, so a tool that seems to need the loop is a tool that needs another value in its `ToolCtx`.

**`ganja-provider`** — talking to a model vendor: the wires, the credentials they present, and the table that sizes and prices what they serve. A wire turns a `ChatRequest` into HTTP and an HTTP response back into a stream of `ProviderEvent`s; it knows nothing about sessions, tools or storage, and with the engine outside its dependency graph that is the compiler's rule rather than a convention. Anthropic Messages, OpenAI's Responses API, the chat-completions wire grok and Copilot ride, the Connect wire cursor's agent backend speaks, the fake provider, and `compat` — not another wire but the two real ones reached at an endpoint a config named, under the id it named. The cursor wire has no upstream TypeScript to port, so its spec is a recorded live probe: Connect, not bare gRPC — enveloped streams whose failures arrive as in-body EndStream frames — with the framing hand-written over the same reqwest/rustls stack, the messages riding `buffa` (pure-Rust protobuf, license-matched), and ganja's own `.proto` checked in beside its generated module under a drift test. Its Run RPC is a duplex: the request body is held open because the server generates by *asking* — a context exec whose answer is the one system-prompt channel cursor honors, and kv blob get/set served from a per-turn map — and a tool exec (shell, read, write, …) is refused on the descriptor's own throw-and-close pair so the server reads a failed tool and the turn survives (**D486**), while a kv kind outside get/set still fails the turn naming it, never hangs it; the prost/tonic fence that once reserved a separate crate for a gRPC stack retired with the reason it existed. P19 added the gateways: `openrouter` rides the Responses machinery at the vendor's own base under a plain key, every OpenAI-shaped body assumption re-derived against the vendor's reference — the API is stateless, so nothing is replayed, nothing sealed is recorded and no summary is asked for, each drop a refusal to guess recorded in the module doc — three sends now stand beside the drops: `reasoning.effort` off the /effort roster, `tool_choice: "auto"` on a roster-bearing request, and the opt-in `openrouter:*` server-tool rows (D489), whose calls come back as the display-only `ServerTool` part; `opencode` and `opencode-go` (the zen gateway's two tiers, **D488**) are one module registered twice over one key, dispatching per model to whichever of the three existing wires the catalog row's npm hint names — choosing the wire chooses the auth header, which is what the gateway's `x-api-key`-only Messages path requires — with the Messages base stripped of one `/v1` because that wire re-adds it. Everything filed under `openai` speaks Responses and asks it to seal the model's reasoning, which the transcript carries and the next request hands back; the credential picks the *backend* and what the request carries beside the bearer. Two failure channels, never a completed turn; retry only before the first byte — with one bounded exception, D475: an in-body failure that arrived before any content event is retried up to three times, since nothing rendered that a replay could duplicate; credential-travel bounds (no redirects, https-or-loopback) live here. **Auth and the catalog fold in rather than standing alone**: the auth→provider edge is one function (`reachable_in_the_clear`, the OpenAI login's redirect check) against some forty provider→auth references reaching per-provider submodule internals, and the catalog names providers by the ids `auth::storage_key` maps to disk — a boundary between any two of them would carry no invariant anyone would gate. What did *not* move is selection: `select` reads a `Config`, so it stayed in the engine over a facade that re-exports the wires.

**`ganja-core`** — the engine. No terminal dependency (CI-enforced), so it is testable headless and can later be served over a socket. It re-exports the crates beneath it under the module names they always had — `ganja_core::protocol`, `::permission`, `::project`, `::tool`, `::watch`, `::auth`, `::catalog`, and since P25 `::team` — so each split cost no caller a rewrite; new code that wants one of them alone should depend on it directly, as `ganja-cli` does for `auth login`. `ganja_core::provider` is the one facade that is not a bare re-export: the wires left and the half that reads a `Config` stayed, over a glob of what moved.

- `engine.rs` — commands in, an ordered event stream out. Delivery is **per-subscriber**: every subscriber has a bounded queue of its own — a lossless subscriber (`subscribe()`) makes the publisher wait, so backpressure lands on the turn task and never on the render loop, while a droppable one (`subscribe_droppable()`) is evicted with an observable error rather than waited for. The first subscriber inherits everything buffered since the engine was born; later ones join from registration. Every event names its session, filled from the engine's one current-session slot (minted at construction, adopted by the first prompt's row, replaced by resume, re-minted by `NewSession`); a subagent's crossing permission dialogs carry the parent's. **Root turns stay serial** — `TurnSlot`, `Busy`, "one turn at a time" all still hold — but *within* a running turn, consecutive `task` calls in one assistant step now fan out concurrently (capped by `agents.concurrency`, default four) and fan back in on completion order while parts keep call order (**D462**); the pending-reply registry (`session.rs`) lets several children hold open permission or question dialogs at once, queued rather than collided, each answered by id. `Engine::settle` polls the turn slot so a headless caller (`ganja run`) can wait for a turn's tail — its `Stop` hook included — before the process exits; `Engine::jobs()`/`shutdown_jobs()` own the background-job registry `job.rs` describes, torn down on the same exit path as `shutdown_mcp`/`shutdown_lsp`. The event stream is complete — a frontend that applies every event holds exactly what the next `ChatRequest` will carry. Two read accessors serve P14's surfaces from the same estimator compaction trusts: `context_estimate()` (tokens against the catalog window, what the status bar's meter polls) and `context_breakdown()` (the per-category split `/context` renders, computed at the request-assembly seams rather than re-derived by any frontend). Four narrow reload seams — `replace_hooks`, `replace_base_tools`, `replace_skill_roots`, `replace_environment` — exist for exactly one caller, the `/plugin` dialog's Reload (**D474**; the skill-roots seam joined in P23 so a reload swaps the `$` expansion's roots beside the rebuilt skill tool): each swaps state a turn clones at its start, so a running turn keeps what it began with and the next one reads the reload. **They take `&self`, over `std::sync::Mutex`** (D474 amended in P25/W7): a lead session hands its engine to a socket server and to its own loop at once, so `Engine` had to become shareable, and after the change **no `&mut self` method survives on it** — the frontend holds an `Arc<Engine>`. The rule that makes that safe is machine-checked rather than reviewed: no guard lives across an `await`, which clippy's `await_holding_lock` asserts at `-D warnings`.
- `session.rs` — the agent loop. Mark a step, ask the model, execute the tools it called, repeat until a request ends without tool calls. **Tool results are information, never control flow**: a refusal, an unknown tool, unparseable arguments or a failed tool all become error text the model reads next, and the loop continues. Only a cancel or a dead provider ends a turn early; there is no step cap. A step's calls resolve through four phases (`prepare`→`start`→`body`→`finish`) so a batch of concurrent `task` calls and an ordinary sequential call share one code path — only `body` ever leaves the turn's own thread. `crate::hook`'s nine events fire from five seams here: `PreToolUse`/`PostToolUse` bracket a call (a `PreToolUse` block, exit 2 or `permissionDecision: "deny"`, routes the same `fail_call` a denied rule does; `"allow"` skips the dialog and never overturns a stored deny, **D458**), `Notification` brackets a permission or question wait, `Stop` fires at the turn's tail before the slot releases, and `PreCompact` fires before compaction. `Turn.hooks` is inherited by `Turn::child`, so a subagent's own tool calls are gated too, and `subagent.rs` fires a child's own `SubagentStop` once its turn and its watcher both resolve. A prompt's or steer's `$name` invocations expand here as well, at the same seams mentions resolve at (**D491**): each name becomes the `skill` tool's own `<skill_content>` rendering — `skill::rendered`, shared and never copied, pinned by a byte-identity test — or its not-found sentence, off the roots `Engine::with_skill_roots` installed; a miss is information the model reads, never a dead turn, and no permission dialog or hook fires because the person typing the token is the consent.
- `hook.rs` — commands a config runs at those nine named moments (`PreToolUse`, `PostToolUse`, `UserPromptSubmit`, `Notification`, `Stop`, `SubagentStop`, `SessionStart`, `SessionEnd`, `PreCompact`), spawned under the same POSIX shell `bash` uses with the event's JSON envelope on **standard input** — this module owns its own spawn because `ShellTool`'s hard-codes `Stdio::null()`. Every hook matching one event runs concurrently and is awaited under its own timeout; exit 0 passes and its stdout is read as the documented envelope or as plain text; exit 2 blocks where blocking means something (`HookEvent::blocking` is exactly `PreToolUse`/`UserPromptSubmit`); anything else — including a timeout kill — is a non-blocking failure reported through `tracing` and the returned `Outcome`, never through a new protocol event (**D459**). A hook runs with the **user's own authority** and crosses no permission dialog — the config file it came from is the trust boundary — and never fires for another hook, by construction rather than a guard. No upstream opencode counterpart at all (**D456**); the stdin envelope omits Claude's `transcript_path` because this build's transcripts are SQLite, not JSONL (**D457**).
- `job.rs` — background shell jobs: a `bash` call started with `run_in_background: true` returns immediately and keeps running under `JobRegistry`'s own root cancellation token, a child of nothing a turn owns, so a turn's cancel never kills a background job. `bash_output` drains whatever a job wrote since the caller's last poll (one cursor per job — this port's shape has exactly one caller per job) with an optional regex `filter`; `kill_shell` ends the whole process tree. No upstream counterpart (**D454**); the `timeout` argument the tool shares with the foreground path is silently unconsulted on this branch, by design — backgrounding exists precisely so a call can outlive the foreground default (**D455**). The status bar's `N bash running` segment polls `Engine::jobs()` on every tick.
- `teammate/` + the teammate half of `subagent.rs` — P25's landing: agents that outlive the call that started them. `TeammateRegistry` is `JobRegistry`'s shape for `JobRegistry`'s reason — its cancellation token is a child of nothing a turn holds, so cancelling a turn never ends a teammate — differing in what a shutdown *waits* for, since a teammate's turn is settled rather than killed: its transcript is a session somebody may open tomorrow (**D500**). One trait, six named backends of which **all six spawn** (**D501**, as amended by Dv-7): `in-process` (a whole second engine and session in this process), `ganja` (the default — a ganja in a tmux pane; named `pane` until P27's Dv-1, 2026-08-20) and `claude` (a real `claude` binary in one), plus P27's `codex` and `grok`, headless children of somebody else's CLI, and `agy`, which ships write-capable at an honestly-stated posture after a user directive reversed its measured no-ship (**D507**, **D511**, **Dv-7**) — all chosen by name and refused by name, never silently fallen back to — so a default spawn in a session with no reachable tmux refuses by name rather than downgrading to `in-process`. Both doors — `task`'s `name` argument and `/team spawn` — call one `spawn_teammate` (**D504**). Panes are laid out as one column beside the lead, filling downwards — the first teammate splits the lead `-h` at 70%, leaving it 30%, and every later one splits the bottom of that column `-v`; which of the two a spawn gets is read off tmux's own geometry (`Server::column_bottom`) rather than remembered, because a teammate's pane may hold a foreign CLI that no `ganja` argv predicate would match and because a person may have closed one (user directive, 2026-08-20). A pane inherits an **enumerated** environment rather than the parent's (**D502**), and its launch line is ≥2 words on purpose: tmux runs a one-word command through `$SHELL -c`, which sources rc files and re-imports exactly the credentials the enumeration left out. `reaper.rs` ends an orphaned pane only on what it is **running** — argv carrying both `--agent-id <id>` and this lead's `--parent-session-id` — never on what was recorded, because a pane id is recycled (**D506**). The ring of recent calls each teammate's row shows is registry state read off a **droppable** subscription and surfaced through `ganja-protocol`'s own `MemberView` (**D503**); it is emphatically not written into `MemberRecord`, which is Claude's document. `lead_inbox.rs` is the lead's §6.2 pass — a UI hook, not a service: it reads, classifies and prunes, and hands a frontend what it has to act on, which is why the tick owns the cadence and this module owns none.
- `teammate/shim.rs` (with `shim/records.rs`) + `codex.rs`, `agy.rs`, `grok.rs` — P27's landing: a teammate that is somebody else's CLI. Neither upstream opencode nor Claude Code runs another vendor's agent as a teammate, so every sentence here is ganja's own; and none of it is a fourth provider — a shim teammate runs the foreign CLI's own agent, its tools and its sandbox, and this side only carries words in and words out. **Three named shim backends over one two-shape core** (**D507**) — codex and grok as `Shape::PerMessage`, one child per inbox message; `Shape::Resident`, one child for the member's whole life fed a line of NDJSON per turn, is the shape agy is driven on. A per-CLI module answers only what that CLI spells differently. The core's loop mirrors `runner.rs`'s and adds one guard it does not have, because of what the two deliver into: an in-process teammate reading odd JSON is a model reading odd JSON, while a shim pasting the same text into a foreign agent's prompt is ganja's internals leaving the building — so **any inbox message whose text parses as a JSON object bearing a `type` key is not prompt material**, total by construction because `Frame` is `#[serde(tag = "type")]`, and implemented on a predicate that can tell "no `type` key" from "a kind this build has never heard of", which `reserved_kind` cannot. The runtime holds such a member as a `Handle::Child`, and the record it writes into the teams directory is a `Surface::Shim { cli }`: a per-CLI `backendType`, beside the **in-process sentinel** in `tmuxPaneId` — a lie an older ganja and a real `claude` both survive, where a fabricated pane id would aim pane machinery at a process that has none, at the cost of one lossy direction on the read back (a pane-mode shim writes the **real** pane id there instead, D512, lossy the other way and for the same safety reason). **What a person consents to** (**D508**): v1 grants a shim teammate **read, and not write** — except agy, whose floor **Dv-7** extended to **write-capable** on a user directive, at the honest posture enumerated below. There is no escalation tier at all — a `SpawnSpec` carrying `bypass` is refused by name rather than quietly downgraded, since answering "yes" while doing "no" is the one outcome somebody who typed `--bypass` cannot have wanted — the posture is pinned on **every** turn including resume (two `-c` keys for codex, because `codex exec resume` carries no `-s` and this machine's own config reads `danger-full-access`; one axis for grok, whose `--sandbox read-only` is the bound while the `--permission-mode dontAsk` beside it is labelled with what it measurably does rather than credited as an approval axis it never reaches), and every shim spawn raises `Ask` on its own always-asking `teammate_foreign` permission, which no stored allow can silence. The grant is described by **what it bounds**, and where the floor is large by what it **enables**: codex's row reads "writes denied, whole-disk read, network denied — may read any file you can, including credentials, but has no network to send them over", grok's "writes denied outside `~/.grok` and temp, whole-disk read, no network bound (macOS) — may read any file you can, including credentials, and may send them anywhere; reading takes no approval, and a tool request that needs one ends the turn", and agy's — the only row describing the **absence** of a bound, and therefore the clause's own case — "sandbox: terminal bounded, no enforced filesystem bound — may read any file you can, including credentials, and write anywhere you can; those writes are outside the snapshot chain /undo walks". Two rules fall out of that shape, and in P27 both bit: **a floor nobody can state in a priceable sentence does not ship**, and **no posture row, dialog sentence or ring line ships ahead of that CLI's own gating probe**. **What a child is, and what ends it** (**D509**): headless child processes, never panes and never `send-keys` — the sealed `tmux` crate is untouched — (**amended for the shim trio by D512**, the next bullet: since P28 every shim spawn door opens the CLI's own TUI in a pane, and the headless door stays in the tree reachable by the testkit alone) and a **headless** shim is the only teammate shape whose *turn* is bounded, deliberately, because it is the only one whose progress ganja cannot observe: an in-process teammate streams events into this process and a `ganja` pane is a ganja whose status bar a person can look at, while a shim child that has stopped writing to a pipe is indistinguishable from one that is thinking. One mechanism bounds both shapes, the *value* is per CLI — codex's and grok's derived from their own probes' wall-clocks, agy's from an *ordering* against its own `--print-timeout`, which the driver then re-derives from the effective deadline so one config key moves both — and one curated `teammates.shim_turn_timeout` moves all three headless drivers so the number is never a source edit — a key that since D512 governs that headless machinery alone, no spawn door reaching it. Orphans are handled without trusting what a record claims: own process group plus `kill_on_drop`, stdin EOF as the first line, and a `/tmp/ganja-<uid>/<stem>-<lead pid>.shims` record whose first lines carry a version token and the recording lead's own `(pid, start-time)`, written `tmp`+`rename(2)` and compared through a `ps` pinned to `TZ=UTC`/`LC_ALL=C` so an identity token cannot drift under a live process; the sweep is governed by **"no owner proof, no signal"** — a file whose owner is live is left entirely alone, and an owner that cannot be proven gone is a log line rather than a branch — and it runs where `reaper::sweep` does not, being ungated on a reachable tmux server. `socket::prepare_directory` moved down into `ganja-tool` for it, **amending D505's split**. **How each CLI is spoken to** (**D510**): codex is `codex exec` then `codex exec resume`, prompt on stdin, every `AgentMessage` item in the JSONL forwarded as its own mail in order; grok is one child per message reading the Messages-NDJSON output — a shape this repository already speaks fluently, where that vendor's ACP output would be a new protocol surface adopted for one CLI — with a v7 UUID minted on the first turn's line, so `--session-id` can never title-match somebody else's conversation, and that id echoed back off the child's own `init` record for the resume, and the prompt in a `0600` file whose *path* the argv names, because argv is world-readable through `ps`. agy's resident stream-json wire is `{"event":"user","message":{"content":"…"}}` in and `init`/`step_update`/`result` out — keyed on `event` rather than `type`, with the terminal `result` the only record carrying any text, so a lead hears from an agy teammate once per turn. **What measurement changed** (**D511**): the plan was written before any probe ran, so six deviations are recorded in its own ledger rather than quietly absorbed — the `pane` backend renamed `ganja` and made the default (Dv-1); `Surface::Foreign`/`ForeignCli` renamed `Shim`/`ShimCli`, because a `claude` binary is foreign too and what actually distinguishes these three is that they do not speak the mailbox (Dv-2); two resume-story clauses ruled narrower than they read, since under v1's name rules a retired member's name is never reused and so the identity a "context lost" mail would report on cannot exist (Dv-3); one crash-stranded staging file accepted as residue rather than raced away from a concurrently live lead (Dv-4); codex's `-c`-versus-profile precedence question found *unbuildable* rather than unbuilt, a config-selected profile being refused at load by the probed version (Dv-5); and agy's prescribed launch line found unparseable, `-p` taking the prompt as its flag value (Dv-6). Three of the verdicts are the probes' and not the plan's — **codex ships**; **agy did not, and then did**: its `--sandbox` bounds the terminal only, and in the same runs where a shell write outside the working directory was denied, agy's own `write_to_file` tool wrote to an absolute path outside it in 2 of 2 runs of that flag set — measured grounds for a no-ship, which a user directive then reversed (**Dv-7**), shipping it as a **write-capable** teammate whose posture row says exactly that and adds the clause a reader could not derive, that those writes are outside the snapshot chain `/undo` walks; and **grok ships as wedges-with-mail**, because a tool request that needs approval ends its turn, which its row, its dialog and its ring say rather than hide — a label sized to 1.0.6, where every tool ask wedged; at 1.0.7 only a shell write the sandbox would deny still asks, so the wedge is rarer and the label stays because the mechanism it names did not change (the re-probe below). Each shipped sentence is compared against that CLI's own recording under `crates/ganja-core/tests/fixtures/*-posture-probe.txt` rather than against a second string literal, because two literals agreeing proves only that somebody typed carefully. What P27 left open is carried as beads rather than as prose, and one of them was decided twice: this machine's own `~/.grok` **was** a symlink, and that vendor's read-only profile refuses to start under one, so every grok turn **on this machine** refused with the vendor's own sentence as structured mail — an operational fact about one laptop and not a statement about grok's code (the backend ships, and its refusal arriving as mail is the failure channel working) — which the user first settled by scheduling `GROK_HOME` as a credential-home addition on the `CODEX_HOME` precedent, a D508 amendment deliberately outside P27's scope (`ganja-code-q98`), and then, on 2026-08-20, un-symlinked the directory; grok 1.0.7 ran real turns under the same launch line that day, the live walkthrough P27 deferred was walked under P28 (the D512 bullet and that plan's ruling 16), and the bead now carries only the carve-out a machine that keeps its symlink would still need. codex is the backend P27's own acceptance was walked on. The same re-probe closed one of the behavioral beads: `ganja-code-vaz` (grok's network bound shipped unmeasured, its sentence deliberately over-disclosing) is measured at 1.0.7 — a shell `curl` reaches a public host unasked, headless and in the TUI alike, so "may send them anywhere" is literal, and the live ladder's arm (d) pins it. It also recorded a drift the bound sentence survives: at 1.0.7 a sandbox-*permitted* shell command or write runs unasked where 1.0.6 asked and cancelled it, and only a shell write the sandbox would deny still asks — every bound clause holds, "a tool request that needs one ends the turn" is still what happens to the asks that remain, and what shrank is the **set** of requests that ask (`grok-posture-probe.txt`'s re-probe block; the ladder asserts 1.0.7's answers, and a 1.0.6 binary fails it by design). The other seven stay ordinary follow-ups: the acceptance tail is carried as `ganja-code-qq5`, `ganja-code-zte`, `ganja-code-lp1` and `ganja-code-1o9`, and the behavioral tail as `ganja-code-v69` (a grok child that prints its session id and then dies loses it), and `ganja-code-0tv` and `ganja-code-98v`, the review tails W2 and W3 each left.
- `teammate/shim_tui.rs` + the pane vocabulary in `teammate/tmux.rs` — P28's landing (**D512**, `.omc/plans/2026-08-20-shim-native-tui-panes.md`, user directive 2026-08-20): a shim teammate is its CLI's **own native TUI in a tmux pane** beside the lead, spoken to through bracketed paste — not a log viewer and not the headless child. Neither upstream opencode nor Claude Code has a counterpart, so every sentence is ganja's own; and it **amends D509's "never panes and never `send-keys`"** for exactly these three backends while leaving the sealed `tmux` workspace crate untouched (every tmux word here is `ganja-core`'s own control client). `Engine::with_teammates` wires all three shim slots to one `ShimTui` over a `TuiDriver` companion trait defined and implemented in this file over the sibling drivers (the lead's ruling 3: a local trait over sibling types reopens no driver file and adds no second `match MemberBackend`), each answering `tui_argv()` — **the pinned D508 floors and nothing else**, never a prompt and never an identity flag: codex `-c sandbox_mode="read-only" -c approval_policy="never"`, grok `--sandbox read-only --permission-mode dontAsk`, agy `--sandbox` — and a `READY_MARKER`, both pinned against that CLI's own recording under `tests/fixtures/*-tui-probe.txt` (codex's composer line `› Ask Codex to do anything`, agy's footer `? for shortcuts`; grok's its composer glyph `❯`, since that composer draws no placeholder — pinned on a second recording taken after the user un-symlinked this machine's `~/.grok`: under the symlink grok 1.0.6 refused its sandbox profile with the headless sentence byte for byte, bead `ganja-code-q98`, and that refusal stays in the fixture as the dead-pane case). **Spawn**: no reachable tmux server refuses **by name**, exactly as the `ganja` backend does — no headless fallback, no config door; the D508 always-ask `teammate_foreign` gate is untouched; the pane is split through the same `split_idle_shell` the two native pane backends share and placed by `Server::column_bottom`; `remain-on-exit` is set **before** the launch line so a CLI that dies leaves its refusal readable; the launch line is `exec <binary> <tui_argv>` through the pinned `launch_line`/`type_line` pair (shlex single-quotes codex's `-c` values, the pane shell strips them, codex reads the TOML bytes exactly — pinned by a round-trip test); and the pane's environment is `CARRIED_ENV` plus the driver's admitted `additions()` (`CODEX_HOME` today), composed through the same `shim::admits` class rule as the headless enumeration — but a pane **inherits the tmux server's environment and `-e` only adds**, so no pane-mode sentence claims the environment is enumerated (D502's claim does not carry). **Readiness is a poll with a ceiling, never a gate**: `capture-pane -J` every 250 ms for 15 s looking for the marker; seeing it is the ordinary case — **held one second** (`READY_SETTLE`) before the first paste, because a composer that has just drawn is not yet one that submits: the W5 walkthrough's first live spawn pasted and pressed Enter 0.2 s after codex's composer line showed and the text sat unsubmitted while the ring said "delivered" (4 of 5 runs at 0.2 s; 6 of 6 submitted at 0.5 s, recorded in `codex-tui-probe.txt` and pinned) — and is then re-checked against a live `panes()` listing so a marker printed on a corpse cannot yield a live member (HIGH-2), nor one that died during the settle; not seeing it is a ring note and a proceed, because a first spawn in an untrusted directory shows a trust dialog and a logged-out CLI its login screen, both answered by a person in the pane — codex's headless `ready()` login pre-check is deliberately not run for a TUI spawn for that reason — and what the timeout changes is **whether Enter is pressed**: a spawn that saw its marker pastes and submits, one that timed out pastes and presses nothing, since an Enter into a pane that may be holding that dialog answers it unseen (HIGH-3; the text waits in the composer for the person). The one failure is the pane's process **dying inside the window** — grok's every spawn under the symlinked-home recording — refused as "the {cli} backend is unavailable: {cli} exited before its composer was ready; its pane said: <vendor's last line>", the last words captured *first* and only then the dead pane closed through `close_dead`, because a dead pane left on screen halves the teammate column on every refusal (ruling 8c; `column_bottom` counts dead panes deliberately). **Delivery**: one inbox message is `load-buffer -b <name> -` with the text on the client's **stdin** (never argv — `ps` is world-readable, the rule grok's prompt file already encodes) then `paste-buffer -p -d -b <name>` **in one client invocation** so the buffer's whole life is one process and a delivery dropped mid-await leaves no cleartext buffer behind (F1, measured: tmux sets a buffer only from a read it saw end), then `send-keys Enter`; the stub receives exactly `ESC[200~ body ESC[201~ \n`, pinned byte for byte. The body is **control-neutralized before it is framed** (`paste_body`): the paste's own close marker is a control sequence, and a peer's message containing one would otherwise end the paste early and land what followed as raw keystrokes in the foreign composer (measured live) — so every control character but `\n` and `\t` is stripped, the invariant `ganja-tui`'s notifier already holds for text inside an escape, narrowed to keep the two a composer reads as content. Deliveries are **serialized per member** (`paste_submit` is not safe against concurrent calls to one pane), retired at write time under `Delivery::FireAndForget` (D503), and a failed delivery is a ring note **and a word back to the sender**, never a blind redelivery — the text may be sitting pasted-but-unsubmitted (ruling 8a/b, MEDIUM-5). **No per-turn deadline in pane mode**, by D509's own rationale: the deadline existed because a headless shim's progress was unobservable, and a TUI in a pane is a thing a person can look at. **v1 is send-only**: the lead's words reach the composer and the lead hears nothing back — replies render in the pane, no transcript is read back into the mailbox (bead `ganja-code-9u1`; codex's own `codex queue` injection door is `ganja-code-tvh`) — and every sentence a person reads says so: the spawn dialog carries `posture` (D508's bound, unchanged and still pinned to `*-posture-probe.txt`) **and** `surface`, the backend's own `surface_line()` — `pane_line`, per CLI, **opening** on one `SEND_ONLY` clause (`send-only: the lead hears nothing back`) — and the `/team` ring opens on `shim::posture_lines` and closes on that same string (`shim_tui::spawn_lines`), not on the headless door's grok `--permission-mode` rider (ruling 5). The clause comes first and the whole row is short because of where it is read: the spawn dialog is the generic permission modal, whose clamp leaves a seventh argument about two rows at its widest, and a ring row is one line cut at the dialog's width — `ganja-tui`'s permission test renders the real sentences at 80x24 and asserts the clause survives for all three (the review of W4 found the first draft's tail-position clause cut off on every backend). Per CLI because the pane-mode fact differs under each CLI's flags, and each says only what its `*-tui-probe.txt` recorded: codex's that under `approval_policy=never` it asks no approval (a denied tool is denied); agy's that it opens in **accept-edits mode: file edits auto-approved** (the probe's own `outcome:` words) beside the Dv-7 write-capable bound; grok's is the row that has to contradict its own bound on purpose: the headless measurement behind `posture_line`'s "a tool request that needs one ends the turn" does not carry to the TUI, where (measured on 1.0.7) the write tool is blocked outright, a proposed shell command raises grok's **own** three-option approval prompt to the person — `dontAsk` silences nothing there — an approved write into the working directory is still denied by `read-only`, and a rejection is what ends the turn; so its row says that grok asks the person in the pane before a tool that needs approval, that the bound holds against their yes, and that only their no ends the turn — and a reader of the spawn dialog sees only the first of those three, which is why it is the one row whose shape is inverted: the spawn dialog is 76 columns at its widest and grok's four-row bound sentence leaves this row exactly one line there, about twenty characters past the send-only opener, so the ask comes first and the "its own TUI in a tmux pane beside you" preamble is pushed behind the facts with the flag's name last; the ask is the fact a person has to act on (a pane waiting for them), so it is the twenty characters that survive, `ganja-tui`'s 80x24 render test asserts they do, and the `/team` ring's wider row carries the bound-holds clause after it while the rest of the sentence is the string and not the screen. Nothing claims a trust dialog or a login screen renders: neither is in a recording. **Record**: `Surface::Shim { cli, pane }` writes the per-CLI `backendType` beside the **real** `%N` in `tmuxPaneId`, so the record reads back as `Surface::Pane` — lossy the other way from the headless sentinel and safe for the mirror reason (the pane exists) — and `reaper::sweep_on` recognizes a shim pane **by `backendType` before the surface match** and leaves its record alone (MEDIUM-4): a foreign CLI's argv carries no `--agent-id`/`--parent-session-id` pair, so the D506 witness cannot match it, and without that recognition a same-session restart read it as `Recycled` and dropped this teammate's own record while correctly leaving the pane (full witnessing is `ganja-code-3nz`; a `ganja shim-host` wrapper restoring reapability is `ganja-code-tss`). A lead that dies leaves its panes, correctly, under "no owner proof, no signal" — unlike a headless child the orphan is a visible TUI a person closes by hand — and no `.shims` orphan record is written for a pane, because that sweep signals only what ganja forked. **A pane that stops running after readiness** — the CLI quit, crashed, or a person closed the pane — is noticed by the member's own loop rather than by the next paste that would have failed into it (`LIVENESS_POLL`, two seconds, four inbox passes; a listing that *fails* proves nothing and retires nobody, the reaper's own rule): the pane's last line is read while the corpse is still on screen (and only from a pane still this member's — an id recycled under another pid is somebody else's screen, never quoted), the messages left in that member's inbox are answered to their senders rather than left unread, the corpse is closed through the same dead-only door with no shutdown asked — and what `end` actually left is what every sentence then says (`PaneFate`: closed; a dead pane tmux would not take away, named as a person's to close; a recycled id, left alone), never assumed — the lead's model is told in prose, and the registry is handed an `Exited` the lead's next §6.2 pass retires the member on — `TeammateRegistry::retire`, the door a `shutdown_approved` takes, so the member leaves the roster and the team file the same way — reported under `Pass::exited` so a frontend says it once (`Exited::notice`, on the status bar or the `/team` dialog's notice line). Carried through the registry rather than as a frame because no frame says it honestly (a `shutdown_approved` answers a request nobody sent; `teammate_terminated` is the lead's word to a teammate). Measured: `paste-buffer` into a dead pane is refused (`target pane has exited`), so a delivery that races the exit takes the failed-delivery road and tells its sender, while one that landed in a composer still drawing is lost in the pty, honestly, under `FireAndForget` (bead `ganja-code-g9u`, landed 2026-08-20 on a user directive that chose retire-and-close over leaving the corpse). **Shutdown** is ruling F3's order: `SIGTERM` to the pane's process group **first, while the pane still pins the pid** and the recorded `(pane_id, birth)` pair can vouch for it (agy survives the `SIGHUP` a bare `kill-pane` sends — measured), wait for the pane to stop listing live, `SIGKILL` if it did not, and only then `close_dead` what remains; signalling after the kill would signal a pid the kernel may have handed to somebody else. **Ruled mid-flight and recorded in the plan's own ledger** (rulings 1–12): `capture` adds `-J`; named per-call buffers because the unnamed stack is server-wide and D462's fan-out makes concurrent deliveries real; `Server::panes()` skips dead panes (a remain-on-exit corpse lists with an empty `#{pane_pid}`) and deadness is `#{pane_dead}`'s answer, never an absent pid's; `Closed::Refused` exists so a dead pane whose kill did not take is not reported alive; and a `paste`-only door exists beside `paste_submit` for the timed-out case. A silent-composer spawn holds the `/team spawn` door for the 15 s readiness ceiling, by design (the door is the team file's single writer; a second spawn meanwhile reads `BUSY`). What P28 left open rides as beads: `ganja-code-0zd` (`gone_within`'s 50 ms poll cadence), beside the four above; `ganja-code-g9u` (a TUI that dies *after* readiness) landed the same day, above.
- `provider.rs` — half a module, and the half that reads a `Config`: `select`'s four-tier chain, `Selection`, `selectable`, the `Wire`/`openai_provider` dispatch that picks a backend by *kind* of credential, and the config→wire translation. The wires themselves are `ganja-provider`'s, globbed in so every path a caller already writes still resolves.
- `mcp.rs` — config-named MCP servers, dialled concurrently in the background at startup; their tools join the registry as `mcp__<server>__<tool>`, every one of them asking by default — the registry name is the one permissions, hooks and transcripts key on; a wire whose vendor refuses that spelling advertises a deterministic alias and translates the model's calls back (`provider/toolname.rs`). The original "dialled once, never reconnected" posture retired (**D463**): `Servers::reconnect` is the `/mcp` dialog's manual door for a server the dialog shows as `Failed`; `Servers::retry_once` is a bounded, spawned-and-never-awaited automatic redial — exactly one per server per session — for one whose very first dial never succeeded, run off the existing `refresh_mcp` once-per-turn-start seam; either door bumps `Servers::generation` so only the *next* turn sees revived tools. A tool result over a server's `output_limit` (bytes, default `MAX_CHARS`) is clamped through `truncate::clamp_bytes`, the same spill-file posture every other one-shot tool here uses (**D464**). A remote entry may add `oauth: {}` to discover its own authorization server (RFC 8414), register a client (RFC 7591, falling back to a fixed public id when the server names no registration endpoint), and run the existing PKCE/loopback browser login, storing the result under a reserved `mcp:<server>` credential-store key; the dial sends the bearer proactively and retries once, refreshed, on a `401` that carries `WWW-Authenticate` (**D466**, `ganja-provider/src/auth/mcp_oauth.rs`). No upstream counterpart for any of the three — opencode dials once and refuses the `oauth` key by name. Past the config's `tool_defer_threshold`, what those servers lend is *advertised* selectively (**D492**, minted at `session.rs`'s filter): whole servers' schemas defer largest-first through `ganja-tool`'s one `retain_advertised` filter — the registry, the permission/hook/transcript keys and `/mcp`'s counts stay whole — a per-step block beside the reminders names the deferred, the resident `tool_search` activates a schema for the session (advertised from the very next step), a direct call executes and activates on either outcome, and activations are flushed to the root session row at each root-side call's `finish` (a child's arrive at the parent's `task`-call fan-in), re-seeded on every resume from the row unioned with the full transcript's `mcp__*` call names, and reset by `NewSession`.
- `lsp/` — language servers, opt-in by config and spawned lazily by the first touch of a file they claim. Diagnostics — errors only, pushed and pulled, merged and deduped — are appended to `edit`/`write` results at one seam in `session.rs`; no LSP failure may fail a tool call or a turn.
- `plugin.rs` — Claude Code's `.claude-plugin` plugin and marketplace shapes, verbatim (**D472**, `claude-plugin-spec`; upstream opencode has no plugin system at all), over an install store at `<config home>/plugins/` whose only doors are the explicitly typed `ganja plugin` subcommands and the `/plugin` dialog — install is the consent, because a plugin's hooks and MCP servers run with the user's own authority. Foreign files are read tolerantly (Claude ignores unknown manifest keys, so this parser does too) but every name is refused at parse if it could escape the store, and a marketplace add stages, validates, then moves — no failure leaves half an add behind. At config load each enabled plugin becomes a **config contributor**, merged per surface at exactly one point in `Config::load_with` rather than as a fourth tier through `Config::merge` (**D473**, `plugin-component-merge`): hooks *append* beside config-declared groups, MCP servers arrive namespaced `plugin:<name>:<server>` and ask by default, `skills/` concatenates onto the roots, `commands/*.md` join the `command` table namespaced `<plugin>:<name>` — Claude's own spelling for a plugin command — read by the very loader a project's own `.ganja/commands` goes through rather than a second parser, and agents and LSP entries merge per key with the explicit config winning and the collision reported by name. Six surfaces, and the sixth (commands) closed the set in P22. One collector walks a plugin's components for the listing and the load path both, which is what keeps `ganja plugin list`, the `/plugin` dialog and what a session actually serves one answer.
- `agent.rs` / `instruction.rs` / `command.rs` — who a turn runs as (build, plan, general, explore, plus whatever the config adds), the system prompt it runs under (a base prompt per model family, the `<env>` block, the `AGENTS.md` family — including, since P15, the nested files a touched subtree earns lazily (**D480**) and the opt-in memory section (**D478**)), and the slash commands it can run. A command's template expands the way upstream's does: `` !`cmd` `` runs through the passthrough's spawn at the project root and substitutes its output, and `@file` attaches the way a composer mention would.
- `config.rs` — `ganja.jsonc`/`ganja.json` across three tiers, under the environment and the flags. Unknown top-level keys are refused by name. `hooks`, keyed by event name, is Claude's own `{matcher, hooks: [{type: "command", command, timeout}]}` shape kept verbatim for migration familiarity, refused unknown by event name and by handler `type` alike; `agents.concurrency` (default four, refused at zero) caps how many `task` calls one assistant step runs at once — its own settings object beside `agent`'s per-name map, not a key inside it, since a `concurrency` key living in that `BTreeMap<String, AgentConfig>` would collide with an agent actually named that (**D462**); an MCP entry's `output_limit` (bytes, not the catalog's tokens) and a remote entry's `oauth: {}` marker are the two other P13 additions, all curated and refused by name at load like everything else here. P15's `memory` (bool, absent = off) is the opt-in for the per-project auto-memory section (**D478**); P16's statusline roster gained the `rate` element (**D484**); P17's `effort` (string, absent = none) seeds a *fresh* session's catalog effort through `Engine::seed_effort` — a default, not an override, so a resumed row's own effort wins, and a name the selected model does not serve clears with a log line at adoption rather than refusing the config at load. P20's `openrouter` table (`server_tools`, a list validated against the vendor's documented roster and refused unknown by name) is the opt-in for that gateway's server-run tools, which bill per call (D489). P24's `tool_defer_threshold` (unsigned integer, absent = 32) is the advertised `mcp__*` schema budget (**D492**): past it whole servers defer largest-first, `0` defers every server and a huge value disables deferral — counted in tools, the unit `/mcp` itself lists, and top-level because a key inside the `mcp` table would collide with a server named `defer_threshold` (the D462 lesson). Size is not coldness: the deferred giant may be the main server — its tools return to the roster the moment they are touched, and raising this key is the one-line off switch. P27's `teammates` table holds exactly one key, `shim_turn_timeout` (seconds, absent = the per-CLI default, refused at zero): the per-turn deadline a **headless** foreign-CLI teammate runs under, plural for `agents`' reason and a table of its own because it is about how this session *runs* the teammates it spawns rather than about what they are (**D509**). Since P28 (**D512**) it governs that headless machinery only, which no spawn door in this build reaches — a pane-mode shim has no per-turn deadline — so the key is kept curated (a file already carrying it must still load) and is read by nothing a spawn reaches. P14 added the curated `tui` table — `notifications` (bool or event filter), `notification_method` (`osc9`/`bel`), and `statusline` with its named-element roster, an unknown element name refused at load by name — the one config surface that exists purely for the terminal frontend, still parsed and refused here because this file is where every key is either curated or refused.
- `storage.rs` / `snapshot.rs` — versioned session storage in one SQLite database per project, converted from the old file tree on first open — unless its ids predate UUIDv7 (**D493**), in which case the whole tree, or the database with its WAL, is set aside as `*.preuuid-<millis>` under a flock'd `sessions.db.quarantine.lock` and a fresh store takes the name; nothing is deleted — and the working-tree snapshots `/undo` walks. Credentials and the model catalog moved to `ganja-provider` and are still named here as `ganja_core::auth` and `ganja_core::catalog`.

**`ganja-tui`** — every pixel, no engine logic. It links `ganja-protocol` for the types it renders, `ganja-permission` for the stored rules it loads and hands to the engine, and `ganja-tool` for the one thing it runs in-process, the `@` menu's glob walk. One `tokio::select!` owns all mutable UI state; `App::handle` is the only mutator, which is what makes components testable without a terminal. Frames coalesce to 16ms for streaming bursts; a keystroke redraws immediately. Themes are loadable data (four ported from upstream, plus whatever `<config home>/themes/` holds), the palette and the `/` menu are two views of the same command set, `@` raises a file menu, `$` raises a skill selector (the Codex CLI's grammar, **D491**: a mention like `@`, never a line prefix — Tab/Enter complete `$name `, prose dollars like `costs $5` raise nothing because only a discovered name keeps the menu up, and `/skills` is the listing dialog whose Enter inserts the token), and a leading `!` hands the line to a shell. Both inline menus complete on Tab as well as Enter — the file menu inserts the selected path exactly as Enter does, the slash dropdown completes the buffer to the command name **without** running it (Claude Code's own screenshots; upstream's Tab dispatches the row like Enter, D446) — and Ctrl+L forces the next frame through a full redraw, the same `stale` flag the external editor's return path already sets (D445, no upstream counterpart). `/copy` and `/copy-message` put the conversation or the last reply on the clipboard — through arboard and upstream's OSC 52 escape both, so a copy lands even over ssh or tmux — pasted text arrives whole through bracketed paste, a drop or paste of one or more existing (or `file://`) paths becomes `@`-mentions instead of raw text (every token must resolve or the whole paste stays literal), Ctrl+V pastes a clipboard image as a PNG encoded in-process and attached through that same mention pipeline — shown in the composer as Claude Code's own `[Image #N]` token since 2026-08-15, the number mapped back to the saved file at send time while the token stays in the text; a Finder-style **file copy** is read off the pasteboard's file list *before* its decoy basename text — Claude Code reads the same metadata as `«class furl»` — its image files tokenized against the copied file itself, everything else classified as a drop would be; and an **empty** bracketed paste — Cmd+V over an image-only clipboard, where the terminal has no text to put inside the envelope — routes to the same clipboard chain, Claude Code's own mechanism for the system paste chord (D449; D111 now names only the long-paste-folding half still unbuilt), and a configured MCP server that cannot be reached is named in the status bar. The composer keeps upstream's prompt history (fifty entries on disk, walked with Up/Down from an empty or unedited buffer), breaks lines on `shift+enter`/`ctrl+j`, and its `@` mentions name line ranges (`@file#5-9`) and attach images and PDFs, with a submit-time warning when the selected wire cannot carry one. Ctrl+R opens a fuzzy-filtered, newest-first search over that same prompt history with a full preview of the entry under the cursor (Claude Code's own panel; upstream's Ctrl+R is unrelated — `session_rename`, which ganja has never bound, D447). A message typed while a turn is already running does not wait for the next one: it steers, sent as `Command::Steer` and rendered in a queue strip above the composer until the engine drains it into the running turn at its next step boundary (`Event::SteerConsumed`); what cannot steer — a refused or still-unconsumed entry, a slash command, a shell line — falls back to a FIFO replayed once the engine goes idle again (D450). `/rewind`, and a second Esc within 500ms of the first at an idle composer (Esc alone still cancels a streaming turn, D452), open a two-step checkpoint picker over the session's own user messages — Claude Code's presentation on upstream's message-level revert — whose second step chooses `RevertScope::Both`/`Conversation`/`Files` (D451) before sending `Command::RevertTo`. The question dialog takes a typed answer unless the tool said `custom: false`, and `/effort` picks a catalog effort for the active model, shown beside the model name in the status bar. Ctrl+T raises a full-terminal-takeover inspector — three tabs (expanded transcript, raw event log, per-turn token table) switched by Left/Right or 1/2/3, no border or centering — over whatever `themes_open` gave up to it: that action's own default chord is now empty, reachable only through `/themes` or the palette (**D453**; the presentation is a synthesis of the OpenAI Codex CLI's own overlay for the takeover and banner, and Claude Code's Ctrl+O mode for the one-line footer). `/mcp` is a new two-step dialog — one row per configured server (status glyph, tool count, first error line), Enter opens a per-row action menu offering Reconnect for a `Failed` row and Login for any row whose server is configured with `oauth` (**D465**), status kept live by the same poll-on-`Tick` pattern every other status surface here already uses. The status bar gained three appear-only-while-nonzero segments: `N bash running` (background jobs), `N tasks running` (shown only above one — a lone `task` call is already named by the activity segment), and `N dialog(s) queued` (a permission or question wait behind the one dialog on screen) — all three read off polled engine state rather than tracked as tallies of their own (**D462**). Esc Esc at an idle composer now enters a Codex-style backtrack walk rather than the rewind picker (**D467**, amending D452's gesture while keeping its both-presses-idle guard): the newest user message lights up in the transcript, each further Esc steps one older, Enter reverts the conversation to before it **and hands that prompt back to the composer for editing**, and any other key exits without reverting — `/rewind` keeps the Claude-style two-step picker. A curated `tui` config table drives two more surfaces: `tui.notifications` (a bool or an event filter of `turn-complete`/`approval-requested`) writes one OSC 9 — or, as `notification_method: "bel"`, one BEL — to the terminal when a turn ends or a dialog waits **while the terminal is unfocused**, gated off the focus events crossterm already reports (**D468**, the Codex CLI's `[tui] notifications` behavior); `tui.statusline` turns the status bar into an OMC-HUD-shaped roster of named elements — user-ordered, width-aware, meter-shaped context segment included, with optional git and detail lines — whose absent-config default renders exactly the fixed bar this build always drew (**D469**). `/context` renders Claude Code's per-category grid over `Engine::context_breakdown()`'s estimate, degrading to honest totals on an unsized model (**D470**); `/usage` renders session totals, cache and reasoning splits and the inspector's per-turn table over what this side already holds, and real plan-limit meters where a backend serves them — the codex backend's 5h/weekly used-percent windows and Copilot's quota snapshots, off every response's headers (**D485**) — with the honest-absence tail (**D471**, narrowed again) naming only the credentials that serve nothing. `/plugin` is a two-step dialog over the plugin store in `/mcp`'s shape (**D474**): one row per installed plugin (state, origin marketplace, per-surface component summary off the same collector `ganja plugin list` prints), Enable/Disable/Remove per row, and Add marketplace / Install / Reload as row-independent actions — the Add/Install text taken by a TUI-local free-text step that reuses the question dialog's rendering and never rides an engine `question` round trip, a failed clone's captured git stderr surfaced on the dialog's own notice line, and Reload reporting the honest split: hooks and skill roots rebuild in-session through the engine's narrow replace seams, while agents, MCP dials and LSP servers are named "restart required" rather than half-reloaded. The transcript pane itself now draws **Claude Code's own grammar, taken from a screenshot** (**D487**): `>` on a prompt, `●` on every reply block and every tool call with its arguments condensed onto that one line (`● Edit(filePath: "a.rs")` — capped, and admitting the cut), `⎿` on what a call answered with its clamped preview hanging under that marker's own measured columns, and `✻` on thinking. A call's state is told by color rather than by a bracketed word, so its header reads identically running and settled; the clamp hint names ganja's own expander (`… +N lines (ctrl+t to expand)`, where Claude's names ctrl+o). Two tools are deliberately special-cased: a delegated `task` keeps upstream's facts in the new dress — and, while it runs, hangs the child's own recent calls under the row off a capped log the watcher writes into the part's metadata, the full log one `/copy` or transcript expansion away (2026-08-15) — and a `read` shows `● Read(/abs/path · lines A-B)` and `⎿ Read N lines` with **no preview at all** — the file's content is what the model asked for, not something to read back to a person through a four-line window. While a turn runs, a strip **pinned above the composer** — outside the scroll entirely, sized fresh into `App::draw`'s vertical split each frame — carries `✻ <verb>… (Ns · ↓ N tokens)` painted its own orange with a brighter band sweeping left to right, and under it the running turn's newest `todowrite` checklist; a settled call's `●` answers the verdict alone — the theme's success green, or its error red with the failed heading kept prose (three 2026-08-15 screenshots, amending D487's tail seam). Its verbs are ganja's own and its token figure the session's running output total, dropped rather than shown as a zero. `/copy` keeps upstream opencode's markdown shape throughout: the screen and the clipboard are read by different readers, and only the screen moved. P25 gave this frontend both sides of a team. As a **lead**: `/team` is a two-step dialog in `/mcp`'s exact shape (**D504**) — one row per member with the ring of what it most recently did under it, Enter opening that row's actions, and a Spawn row belonging to the team rather than to any member; a `/team spawn w1 --backend ganja` line reaches the same spawn sequence, since the palette has nowhere to type arguments — but since 2026-08-20 it raises no dialog of its own: **only asking for the roster** (`/team`, `/team list`) opens the overlay, and a line that already said in full what it wanted is answered on the status bar instead of behind a list nobody asked for. What had to travel with that is the sentence a finished spawn is reported with — `Spawned::notice`, one spelling for both surfaces — because the half of it that must not be dropped is Resolution 4's disclosure that the prompt is on disk in cleartext. A teammate's message arrives as a `PartBody::Peer` and never as text (**D495**), so nothing can mistake it for something the person typed — it rides the D450 steer lane, renders in the queue strip in the peer's own row that Up will not hand back, and is retired on a fact where the backend gives one and at write time where it does not (**D503**). A lead session also asks the binder for its socket, once per id and best-effort (**D505**). As a **member**: `--agent-name`/`--agent-id` put the same app in a pane teammate's role, reading its own inbox instead of leading, forwarding its permission dialogs to the lead as §5's `permission_request` and applying only a `LeadFrame`'s answer — a type a peer's message cannot build.

**`ganja-serve`** — the engine over a socket: the legacy `/session/…` REST surface and a `GET /event` SSE stream, driving the same `Engine` the TUI does and inventing no state of its own — `GET /permission` is the one derived view, kept by a lossless subscriber that only moves map entries. Three postures are pinned by its tests: a non-loopback bind with no password is refused at startup (a deliberate divergence from upstream's warn-and-serve), the launch directory is the only directory served (anything else is `400`, never a silent answer about the wrong worktree), and no query string ever reaches a log line, because `?auth_token=` is a credential in a URL. The SSE stream opens with a `connected` frame, heartbeats every ten seconds, and ends an evicted subscriber with an observable `evicted` frame rather than a silent close; `EngineError` maps `SessionNotFound`→404 and `Busy`→409, with unparseable payloads at 400. P25 gave it a second transport (**D505**, minted here): a **Unix socket per lead session** at `/tmp/ganja-<uid>/<8+ hex>.sock`, `0600`, in a directory the bind refuses unless it is ours at `0700`, with a peer-uid check on every accepted connection. It takes no password — the filesystem already said who may connect — and it serves **exactly three routes**: `GET /global/health`, `GET /team`, and `POST /team/{name}/message` where the name must be the lead. Every session-mutating route and every other read of the session is TCP's alone and answers `404` there, because same-uid is not trusted: `ganja-permission`'s whole premise is that code this user runs — an MCP server, a hook, the model's `bash` — is not the user, and a credential-less socket serving the write API would hand every one of them a prompt into every session on the machine. Both route tables share one `guard` middleware, and a route added to the socket later is a deliberate edit named in `routes::socket_routes`' doc. The *scheme* it binds under is `ganja_tool::socket`'s, re-exported; what lives here is the binder's half — the name walk over a session's candidates, the flock'd `.lock` sibling that says a name is live (never removed, so a full-backlog listener is not mistaken for a dead one), and reclaiming a stale socket only under that lock.

**`ganja-client`** — the other end of `ganja-serve`'s wire, and nothing else: the typed routes and the SSE reader that `run --attach` drives. Its internal dependency list is exactly `ganja-protocol` (CI-gated) — a client that linked the engine would be a second frontend rather than a consumer of the first one's socket. The server's frame vocabulary (`connected`, `heartbeat`, `evicted`, and the event payload) is declared on this side and pinned against a real server; a frame or field outside the declaration is refused readably as version skew, never guessed at, and the credential comes from `GANJA_SERVER_PASSWORD`/`GANJA_SERVER_USERNAME` through serve's own resolver so the two ends cannot disagree about which variable means what. Since P25 it speaks **two address forms over one wire** (**D505**): a TCP base as before, and a socket path — `reqwest`'s own `unix_socket`, so no new dependency and no second HTTP stack, at the cost of one rule the type carries, that a `Client` routed at a socket is routed at *that* socket and is never shared across paths. `ganja sessions --live` health-checks through this form, which is why the crate's internal list is still exactly `ganja-protocol `.

**`ganja-cli`** — clap; no subcommand starts the TUI, one of them — `run` — takes a whole turn without it, driving the same engine headless and writing either readable lines or one JSON object per event (or, under `--attach`, the same account of a turn taken on a running `ganja serve`), and another — `serve` — puts the same engine behind HTTP and SSE until a signal ends it. It is also where the **binder** lives (**D505**): the binary is the only member that links both the terminal frontend and the server, so it is the only one that can hand the TUI a way to bind a socket without `ganja-tui` naming `ganja-serve`. The TUI decides *when* one is asked for — only for a session that leads a team; a pane member and a build with no config home hand it back unused — and `ganja run` binds nothing at all, because a headless turn installs no teammates. `sessions --live` is the reader at the other end of the same scheme, and it unlinks under the name's own lock or not at all.

**`tmux`** — a tmux control-mode client and a second application of this repository's porting rule: `pandaemonium pkg/tmux`, an Apache-2.0 Go package by the same author as this workspace, is the behavioral specification, never source to translate. One persistent `tmux -C` subprocess speaks through piped stdio to an async `Client` built on `tokio::process`; beneath it, the sans-io `Parser` turns raw lines into guarded `%begin`/`%end`/`%error` response blocks and typed `%` notifications — the twenty known kinds, a synthesized `%protocol-error`, and a catch-all that preserves a newer tmux's unknown kind. It is consumed by **nothing** in this workspace and consumes nothing of ours, by the P26 user directive (2026-08-18), not by accident: CI checks both the consuming direction (its normal tree contains no `ganja-*` crate) and the producing direction (every other member's normal tree contains no `tmux`, over a member list derived at gate time). That makes it the deliberate exception to the root manifest's path-dependency convention. There is no `[workspace.dependencies]` entry for `tmux` because that entry would be the opt-in handle another crate used to depend on it, and no crate may opt in; the comment after `ganja-tui` in the root `Cargo.toml` explains the absence where a reader would look for the handle. The hard-fail-by-default real-tmux suite is evidence for keeping that cost: unlike Go's `RUN_REAL_TMUX_TESTS` opt-in, it ran at every port commit and found two cases the specification's own tests had never been forced to exercise. First, tmux's command lexer misparses a bare `%<digits>:word` token even though `%<digits>` alone is a valid pane id, so `pane_flow` single-quotes the compound argument the Go test renders bare. Second, a session-scoped subscription produces `-` in all three inapplicable window, window-index, and pane fields — observed live against tmux next-3.8 as `%subscription-changed live-test $0 - - - : x` — while Go validates those fields unconditionally because its real-tmux suite never asks for that scope; this port decodes the sentinels as `Option::None` and completes the roundtrip. Running real tmux on every commit is what surfaced both gaps, which an opt-in suite can structurally leave unseen.

## For AI Agents

### Working In This Directory

- **Respect phase discipline.** P0–P6 have landed: the workspace, the TUI shell, providers, the agent loop with tools and permissions, sessions and compaction, config, agents, commands, themes, the model catalog, the task tool, `@file` mentions, `!` passthrough — and now MCP servers, LSP diagnostics, working-tree snapshots with `/undo`/`/redo`, markdown rendering, the stale-read watcher and the system clipboard. Scope, acceptance criteria and the ADR live in `.omc/plans/2026-08-03-opencode-rust-port.md`, and each phase's frozen contract in `.omc/handoffs/`. Do not build ahead of the current phase — P7 is underway: the `mcp` listing subcommand, SQLite session storage, the windows CI lane (retired 2026-08-12 — windows support is parked for now, and the `cfg(windows)` code left in the tree has no compile signal), the toolchain date-pin, local packaging and `ganja run` — a headless turn with an nd-JSON mode — have landed, and the credential store has room for a login rather than a key; since then `ganja serve` and the `ganja-client` it answers, the OAuth logins (ChatGPT, grok, Copilot, and cursor's browser-and-long-poll pairing), the websearch/skills/question tools, the cursor wire — Connect, duplex, its context and kv asks answered — and now both plan doors have landed too — `plan_exit` ported, and `plan_enter` **synthesized** from upstream's description and permission vocabulary over a tool body upstream never wrote (**D477**). Outside P7's own numbering, P12 (`.omc/plans/2026-08-11-claude-composer-port.md`) has also landed: seven composer behaviors — Tab completion in both inline menus, Ctrl+R history search, clipboard image paste, mid-turn steering, drop-to-mention, Ctrl+L redraw and the `/rewind` checkpoint picker — five ports of upstream behavior plus two Claude Code presentation divergences (D445–D452). P13 (`.omc/plans/2026-08-11-claude-runtime-port.md`) has also landed waves W1–W5b — the Ctrl+T inspector, background shell jobs, hooks, parallel subagents, and MCP reconnect/output-caps/OAuth, none of them ported from upstream opencode (D453–D466) — with its docs/ledger/verify wave (W6) in flight; treat P13 as landed-but-not-yet-closed until that wave's own handoff says otherwise. P14 (`.omc/plans/2026-08-12-claude-codex-surface-port.md`) has also landed waves W1–W6: the Codex-style Esc Esc backtrack, the curated `tui` table with focus-gated notifications and the HUD-shaped statusline roster, `/context` and `/usage` over the engine's context accessors, and the plugin system — `.claude-plugin` store, `ganja plugin` CLI, and the `/plugin` dialog with its honest-split Reload (D467–D474, none of them ported from upstream opencode). P15 (`.omc/plans/2026-08-14-claude-codex-extras-port.md`) has also landed: the interactive `--auto`/`--yolo` bypass trio answering only Ask-raised dialogs (D479), the lazy nested-AGENTS.md walk-in (D480), and opt-in per-project auto-memory (D478). P16 (`.omc/plans/2026-08-14-claude-files-port.md`) has also landed: markdown command and agent files from ganja's two homes — `tools:` compiling to a refused-not-hidden permission wall (D481, D482) — `ganja mcp add/get/remove` with jsonc-refusing staged config writes (D483), and rate-limit visibility off the response headers every wire already receives: per-wire windows behind a polled engine accessor, /usage's Current window section, and the HUD's `rate` element (D484). P17 (`.omc/plans/2026-08-14-usage-meters-cursor-exec.md`) has also landed: a names-only header probe that settled per-credential feasibility, real plan-limit meters off the codex and Copilot header families feeding /usage's Plan limits section and the HUD (D485), cursor server-exec refusal answers over the shipped client's own throw-and-close pair — a tool ask now fails the tool, never the turn (D486) — the idle status bar's standing key hints removed, and a curated `effort` config key seeding a fresh session's catalog effort. P18 (`.omc/plans/2026-08-14-claude-transcript-render.md`) has also landed: the chat pane redressed in Claude Code's transcript grammar — ● bullets, ⎿ results, > prompts, ✻ thinking and the tail working line (D487) — with readable reasoning made a real part end to end (persisted display-only, drawn whole, sent nowhere) and the read row reduced to its counted summary; a provider-run server tool draws in the same ●/⎿ grammar and rides /copy as an ordinary tool line. P19 (`.omc/plans/2026-08-14-zen-go-openrouter.md`) has also landed: openrouter as a builtin on the Responses wire (a port of upstream's own support, no-guess body ledger), and the OpenCode Zen/Go gateway as two probed ids over one key with per-model three-wire dispatch off the catalog's npm hint (D488) — the probe method, findings and live verification recorded in the plan. P20 (`.omc/plans/2026-08-14-openrouter-features.md`) has also landed: openrouter reasoning on the /effort roster the vendor's reference publishes, thinking mapped from that vendor's own event spelling with a response-scoped dedupe, tool_choice sent by the book, and the vendor's server tools behind the curated `openrouter` config table — display-only on a new `ServerTool` protocol part, never replayed, never dialogued (D489). P23 (`.omc/plans/2026-08-15-skill-dollar-invocation.md`) has also landed: the Codex CLI's `$skill-name` invocation (**D491**) — `skills` riding `SendPrompt`/`Steer` as send-time-validated names, expanded engine-side at the mention seams into the `skill` tool's own rendering (shared function, byte-identity-pinned), the typed-`$` selector and `/skills` dialog in the TUI, `ganja skills` and the `ganja run` prompt scan headless — none of it ported from upstream opencode, which has no user-facing skill invocation at all. P24 (`.omc/plans/2026-08-15-mcp-defer-load.md`) has also landed: MCP tool defer-load (**D492**) — whole servers' schemas advertise-filtered past the curated `tool_defer_threshold` behind the resident `tool_search`, direct calls executing and activating on either outcome, session-lifetime activation flushed at each root-side call's finish and transcript-union-seeded on every resume — modeled on Claude Code's ToolSearch with the direct-call departure deliberate; upstream opencode has no tool deferral at all. P25 (`.omc/plans/2026-08-17-teammates-first-landing.md`) is the largest of these and has landed W1–W7: bare-UUIDv7 session ids so two processes started together cannot mint one id (**D493**), the team frame surface and the peer part (**D494**–**D496**), the `ganja-team` crate over Claude Code's own documents and lock protocol (**D497**), the `send_message` tool and its eight-rung ladder (**D498**, **D499**), the runtime — who owns a teammate's turns, the backend trait, the panes and the orphan reaper (**D500**–**D502**, **D506**) — the lead side with its `/team` dialog and observability ring (**D503**, **D504**), and the cross-session socket (**D505**). None of it is ported from upstream opencode, which has no teams, no teammates and no second agent to address; the specification is Claude Code's throughout, read as behavior — no prose of it ships. W8 (docs, ledger, verify) is what this paragraph is part of; treat P25 as landed-but-not-yet-closed until its own handoff says otherwise. P27 (`.omc/plans/2026-08-19-foreign-cli-shim-backends.md`) has landed all six waves: the shim trio named and the spawn gated, the core that bridges a mailbox to a headless CLI, and then the three measurements that decided what actually ships — **codex** seated behind the shim and probe-verified, **agy refused by measurement rather than by policy** and then shipped write-capable on a user directive that reversed it (**Dv-7**), and **grok** shipped as a teammate that wedges with mail (**D507**–**D511**). Neither upstream opencode nor Claude Code runs another vendor's agent as a teammate, so nothing here is ported from either; each CLI's own observable behavior is the specification, and wherever a probe contradicted this plan's pre-probe prose the probe won and the prose was rewritten. P28 (`.omc/plans/2026-08-20-shim-native-tui-panes.md`) has also landed: the shim trio seated in their CLIs' **own native TUIs in tmux panes** (**D512**, amending D509 for those three backends) — floors pinned on the TUI argv, bracketed-paste delivery with the body control-neutralized, readiness as a poll that never gates, no per-turn deadline, v1 send-only and said so in every sentence a person reads, the real pane id in the record with the reaper taught to leave a shim pane's record alone, and TERM-the-group-first shutdown; the headless door stays in the tree, unit-tested, reachable by the testkit alone. Nothing in it is ported from either upstream; the specification is each CLI's own TUI as measured (`tests/fixtures/*-tui-probe.txt`), and grok's walkthrough — deferred while this machine's `~/.grok` was a symlink (`ganja-code-q98`) — was walked on 2026-08-20 against grok 1.0.7 once the user un-symlinked it: the second recording in `grok-tui-probe.txt`, the readiness marker re-pinned to the captured composer glyph, the pane row rewritten to the approval behaviour that recording holds, and the network bound measured (that plan's ruling 16).
- **Check `git status` before editing.** Phase execution assigns **one owner per file**. A dirty file belongs to another lane — do not finish somebody else's work in flight.
- **Port behavior, not code.** Module docs cite the upstream file they port (`//! Spec: upstream packages/opencode/src/tool/edit.ts`). Deliberate divergences are documented at the point they occur, with the reason.
- **Comments explain why, not what** — including in `Cargo.toml`. Match the surrounding density.
- Never pick a dependency version in a member crate; add it to the workspace manifest with its rationale.

### Testing Requirements

The four gates above, all green, before a phase is called done. Unit tests live beside the code in `#[cfg(test)] mod tests`; anything needing a real socket, a real filesystem layout, or process-wide environment mutation goes in a crate's `tests/`.

### Common Patterns

- Test names are sentences about behavior: `a_denied_edit_leaves_the_file_untouched`, `a_second_subscriber_sees_the_same_events_the_first_does`.
- A test that mutates process-wide state gets its **own test binary**. nextest already gives each test its own process, but the separation still holds the line under a plain `cargo test` (which runs a binary's tests on parallel threads) and keeps the intent legible.
- Anything touching stored state redirects `XDG_DATA_HOME` so it cannot read or write the real user's credentials, permissions or spilled output.
- Nothing may render a whole API key; `crates/ganja-core/tests/secrets_env.rs` pins that with a canary.
- Commit subjects are `scope: intent` — the crates touched, then why (`core,tui,cli: let the model act through permission-gated tools`).

## Dependencies

### External

Load-bearing choices, all pinned in the workspace manifest — which is also where each `ganja-*` member crate is declared as a path dependency, with the reason it exists; sealed-leaf `tmux` is the P26 exception described above and deliberately has no opt-in handle there: `tokio` (runtime), `ratatui` 0.30 + `ratatui-textarea` (TUI), `reqwest` with rustls (provider HTTP; no OpenSSL), `secrecy` (key material wiped on drop), `schemars` (tool argument schemas generated from the argument structs), `ignore`/`grep-searcher`/`grep-regex` (ripgrep internals, so glob and grep run in-process instead of shelling out to `rg`), `similar` (unified diffs from `edit`), `etcetera` (XDG paths), `jsonc-parser` (config files in the dialect upstream's are written in, decoded in document order so permission rules keep theirs), `nucleo-matcher` (fuzzy ranking behind the palette), `libc` (the unix calls the shell tool and the anchored file I/O are built on), `insta` + `expectrl` + `assert_cmd` (snapshot, pty and CLI tests). `crates/ganja-provider` is where `secrecy`, `sha2`, `getrandom` and the SSE-facing half of `reqwest` now live. `secrecy` also moved from a dev- to a production dependency of `ganja-core` in P13: `mcp.rs`'s dial-time bearer header is that crate's first non-test use of a secret directly. P25 admitted three for `ganja-team` and the id: `uuid` (v7 only, the one thing worth nobody's hand-rolled clock-plus-counter — and the sole reason `ganja-protocol`'s external allowlist is three names rather than two), `indexmap` (the ordered map under the `#[serde(flatten)]` passthrough, which is what lets a foreign key survive a rewrite *in position*), and `backon` (the retry ladder, because the lock schedule is a peer's literal `{retries: 10, minTimeout: 5, maxTimeout: 100}` and reproducing it by hand would be reproducing somebody else's arithmetic). The socket transport added nothing: `reqwest`'s `unix_socket` was already in the pinned version.

<!-- MANUAL: Any manually added notes below this line are preserved on regeneration -->

<!-- bv-agent-instructions-v3 -->

---

## Beads Workflow Integration

This project uses [beads_rust](https://github.com/Dicklesworthstone/beads_rust) (`br`) for issue tracking and [beads_viewer_rust](https://github.com/Dicklesworthstone/beads_viewer_rust) (`bvr`) for graph-aware triage. Issues are stored in `.beads/` and tracked in git. Current `br` workspaces normally export `.beads/issues.jsonl`; older `bd`/legacy workspaces may use `.beads/beads.jsonl`. `bvr` auto-discovers the supported JSONL files, so agents should use `br`/`bvr` commands instead of hard-coding a single filename.

### Using bvr as an AI sidecar

bvr is a graph-aware triage engine for Beads projects. Instead of parsing .beads/issues.jsonl / .beads/beads.jsonl directly or hallucinating graph traversal, use robot flags for deterministic, dependency-aware outputs with precomputed metrics (PageRank, betweenness, critical path, cycles, HITS, eigenvector, k-core).

**Scope boundary:** bvr handles *what to work on* (triage, priority, planning). `br` handles creating, modifying, and closing beads.

**CRITICAL: Use ONLY --robot-* flags. Bare bvr launches an interactive TUI that blocks your session.**

#### The Workflow: Start With Triage

**`bvr --robot-triage` is your single entry point.** It returns everything you need in one call:
- `quick_ref`: at-a-glance counts + top 3 picks
- `recommendations`: ranked actionable items with scores, reasons, unblock info
- `quick_wins`: low-effort high-impact items
- `blockers_to_clear`: items that unblock the most downstream work
- `project_health`: status/type/priority distributions, graph metrics
- `commands`: copy-paste shell commands for next steps

```bash
bvr --robot-triage        # THE MEGA-COMMAND: start here
bvr --robot-next          # Minimal: just the single top pick + claim command

# Token-optimized output (TOON) for lower LLM context usage:
bvr --robot-triage --format toon
```

Before claiming, verify current state with `br show <id> --json` or `br ready --json`. `recommendations` can include graph-important blocked or assigned work; only `quick_ref.top_picks` and non-empty `claim_command` fields represent claimable work.

#### Other bvr Commands

| Command | Returns |
|---------|---------|
| `--robot-plan` | Parallel execution tracks with unblocks lists |
| `--robot-priority` | Priority misalignment detection with confidence |
| `--robot-insights` | Full metrics: PageRank, betweenness, HITS, eigenvector, critical path, cycles, k-core |
| `--robot-alerts` | Stale issues, blocking cascades, priority mismatches |
| `--robot-suggest` | Hygiene: duplicates, missing deps, label suggestions, cycle breaks |
| `--robot-diff --diff-since <ref>` | Changes since ref: new/closed/modified issues |
| `--robot-graph [--graph-format=json\|dot\|mermaid]` | Dependency graph export |

#### Scoping & Filtering

```bash
bvr --robot-plan --label backend              # Scope to label's subgraph
bvr --robot-insights --as-of HEAD~30          # Historical point-in-time
bvr --recipe actionable --robot-plan          # Pre-filter: ready to work (no blockers)
bvr --recipe high-impact --robot-triage       # Pre-filter: top PageRank scores
```

### br Commands for Issue Management

```bash
br ready --json                       # Show issues ready to work (no blockers)
br list --status=open --json          # All open issues
br show <id> --json                   # Full issue details with dependencies
br create --title="..." --type=task --priority=2 --json
br update <id> --status=in_progress --json
br close <id> --reason="Completed" --json
br close <id1> <id2> --reason="Completed" --json
br sync --flush-only                  # Export DB to JSONL after Beads mutations
```

### Workflow Pattern

1. **Triage**: Run `bvr --robot-triage` to find the highest-impact actionable work
2. **Claim**: Use `br update <id> --status=in_progress --json`
3. **Work**: Implement the task
4. **Complete**: Use `br close <id> --reason="Completed" --json`
5. **Sync**: Run `br sync --flush-only` after Beads mutations so the JSONL export is current

### Key Concepts

- **Dependencies**: Issues can block other issues. `br ready --json` shows only unblocked work.
- **Priority**: P0=critical, P1=high, P2=medium, P3=low, P4=backlog (use numbers 0-4, not words)
- **Types**: task, bug, feature, epic, chore, docs, question
- **Blocking**: `br dep add <issue> <depends-on>` to add dependencies

### Git Policy

`br` never commits or pushes. Follow this repository's own git instructions before staging, committing, or pushing. If the repository says "commit only when asked," that rule overrides any generic workflow advice.

<!-- end-bv-agent-instructions -->
