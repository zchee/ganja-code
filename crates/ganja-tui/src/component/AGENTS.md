<!-- Parent: ../AGENTS.md -->
<!-- Generated: 2026-08-04 | Updated: 2026-08-04 -->

# component

## Purpose

The three panes the layout draws — transcript, prompt editor, status bar — plus the strip of messages waiting on a running turn, the modals that overlay them: the permission dialog while a tool call waits on the user, the question dialog while the model waits on an answer, the sessions picker while a stored conversation is being chosen, the theme list while a palette is being previewed, the model, agent and effort lists while a session is being pointed somewhere else, the command palette, the reference card, a fuzzy search over remembered prompts, a two-step rewind picker over the session's own checkpoints, a full-terminal-takeover Ctrl+T inspector over the whole frame, a two-step `/mcp` server dialog, and the two inline menus the editor raises — one on a leading slash, one on an `@`.

## Key Files

| File | Description |
|------|-------------|
| `mod.rs` | Module list. |
| `chat.rs` | The transcript viewport: message and part rendering, wrapping, scrolling, tail-following, wheel handling (`WHEEL_LINES`), and the revert state — `revert`/`unrevert`/`drop_reverted` hide, restore or delete the tail an `/undo` took back, with one marker row (`N message(s) reverted — /redo to restore` plus the files) standing in its place. **The grammar is Claude Code's, by screenshot (D487)**: `> ` on a prompt (one caret per message, the rest hanging under it), `● ` on every reply block and every tool call, `  ⎿ ` on what a call answered with its preview hanging under the marker's own **measured** columns (both glyphs are East Asian Ambiguous — `Row`/`lay_out` measure, never count), `✻ ` on thinking and on the working line — which since 2026-08-15 lives in a strip **pinned above the composer** (laid out by `lay_out_working`, drawn by `render_working`, sized into `App::draw`'s vertical split; outside the scroll, so it can neither break tail-follow nor move a pinned viewport), painted its own orange with a brighter band sweeping left to right, this turn's newest `todowrite` checklist hanging under it. State is told by color, not by a bracketed word, so a call's header line is identical running and settled — and a settled `●` answers the verdict alone: the theme's success green, or its error red with the failed heading kept prose. Two tools are special-cased on purpose: `task` (agent, description, progress) and `read` (`● Read(path · lines A-B)` + `⎿ Read N lines`, no preview and none of the tool's envelope — the content is the model's, and the whole of it is one Ctrl+T away). `/copy` (`transcript.rs`) keeps upstream opencode's markdown instead; the two surfaces are meant to disagree. |
| `editor.rs` | The prompt editor — a `ratatui-textarea` `TextArea` with ganja's submit rules layered on top. |
| `queue.rs` | The strip of messages typed while a turn was already running (**F4**): steered entries (`Command::Steer`, cleared on `Event::SteerConsumed`) and the fallback lane — a refused or still-unconsumed steer, a slash command, a shell line — replayed once the engine goes idle. Caps at 5 visible rows; the status bar carries the true depth so a deep queue is never silently clipped. **P25 (D503)** added a third kind of row: a teammate's message waits here too, and it is not the person's to take back — recalling one into the composer would put words nobody typed where Enter reads them as consent, for the `@` mentions, `$` skills and `/` commands in them. So the lane is a **field** on the entry rather than something a caller remembers, and `withdraw_newest` answers with the newest row this person really wrote. An entry is retired on a fact where the backend gives one and at write time where it does not: a real `claude` pane marks a message read when it *reads* it, not when a turn takes it on, so waiting for an acknowledgement it never sends would leave a peer's words pending forever. |
| `status.rs` | The status bar: what the engine is doing (`Activity`), what the session has spent (`Totals`), and — in shell mode alone — the key that leaves it. The idle footer's standing `Enter send · Alt+Enter newline · Esc cancel · Ctrl-C quit` line is gone: those keys do what they do in every terminal program, and shell mode is the one mode whose way out is not guessable. **P13**: three appear-only-while-nonzero segments — `running_jobs` (`"N bash running"`), `running_tasks` (`"N tasks running"`, shown only above one — a lone `task` call is already named by `Activity`), and `queued_dialogs` (`"N dialog(s) queued"`) — all polled off engine state, none tracked as a tally of its own (**D462**). |
| `inspector.rs` | **P13**: the Ctrl+T overlay (**F2**) — a full-terminal takeover (`App::draw` hands it the whole frame and skips the editor/status bar while it is open), three tabs switched by Left/Right or 1/2/3: expanded transcript (reuses the public `transcript::format` renderer, so a completed call's input/output is byte-identical to `/copy`'s), raw event log (a teed, capped `VecDeque<CoreEvent>`, rendered `{event:?}`), and per-turn tokens (a table over retained `TurnUsage` rows with a totals footer equal to the status bar's `Totals::segment()`). A view, not a mode — every render re-reads what `App` hands it, so a streaming turn keeps appending beneath it. Presentation is a synthesis of two screenshots: the OpenAI Codex CLI's own overlay (the takeover, the slash-art banner naming the active tab) and Claude Code's Ctrl+O mode (the one-line footer wording) — filed under **D453**, the same deviation that emptied `themes_open`'s default chord to make room for Ctrl+T. |
| `mcp.rs` | **P13**: the `/mcp` dialog (**F5**) — one row per configured server (status glyph, tool count, first error line), Enter opens a second step choosing an action: Reconnect for a row shown `Failed` (**D463**), Login for any row whose server is configured with `oauth`, whatever its current status (**D466**). Structurally `rewind.rs`'s twin (choose a server, then choose what to do about it) rather than `list.rs`'s `ListDialog` — a row's Enter needs a second step, which `ListDialog` has no room for. Status stays poll-driven (`Mcp::refresh`, driven by a `Tick`-time `App::poll_mcp_dialog()`), no new protocol event. No upstream TUI surface at all to port from — only the `ganja mcp` CLI listing this dialog's data agrees with (**D465**). |
| `team.rs` | **P25 (D504)**: the `/team` dialog — one row per member of this session's team (name, surface, whether it is the lead) with the ring of what it most recently did hung under it, a row's actions behind Enter, and a Spawn row belonging to the team rather than to any member. Two-step shape from `mcp.rs`, row-independent action plus free-text step from `plugin.rs`: the same grammar every dialog here already taught, on a new subject. Two decisions show as code. The ring is **D503's second half** — the in-process backend is the default and has no window at all, so without a surface the most-used teammate would be the least observable one; it is live registry state reaching this component as `MemberView::recent_calls`, ganja's own projection, and never through Claude's member record. And **nothing stands in front of a spawn**: a person typing one is the consent, so the one thing said afterwards is where the prompt they typed came to rest. The spawn request is the `task` tool's own `TeammateSpawn` value, which is what makes a typed spawn and a model's spawn incapable of drifting apart (AC-14); `bypass` sits beside it rather than inside it, because a teammate that wants its dialogs skipped is asked for by a person, never by a tool call. |
| `permission.rs` | The centered modal blocking on one pending tool call. Spec: upstream `packages/tui/src/routes/session/permission.tsx`, trimmed to the one-shot shape `PermissionReply` offers today. |
| `sessions.rs` | The centered modal listing this project's stored sessions to resume. Spec: upstream `packages/tui/src/routes/session/list.tsx`, trimmed to the columns a person picks by. |
| `search.rs` | The Ctrl+R history search modal (**F2**): a fuzzy membership filter (`nucleo_matcher`, matching but not re-ranking) over remembered prompts, shown newest-first with a preview pane for the entry under the cursor. No upstream counterpart — upstream's own Ctrl+R is `session_rename`, which ganja has never bound (D447). Spec: Claude Code's own panel, screenshot 2026-08-11. |
| `themes.rs` | The centered modal listing loadable themes. Owns only the cursor; the app applies the live preview on every move and reverts on Esc. Spec: upstream `packages/tui/src/component/dialog-theme-list.tsx`. |
| `list.rs` | `ListDialog`: one centered modal for choosing a model or an agent — the same dialog over two lists, because what differs between them is the rows and the command Enter sends, and neither of those is drawing. Marks the row the session is already on; previews nothing. |
| `rewind.rs` | The rewind picker (**F7**): the session's own checkpoints — its user messages, newest first, plus a `(Current)` row — each annotated with files-changed or Claude Code's `⚠ No code restore`; Enter opens a second step choosing `RevertScope::Both`/`Conversation`/`Files` (D451) before sending `Command::RevertTo`. Semantics are upstream's message-level revert (`session/revert.ts`); the picker layout and the second-step scope choice are Claude Code's. |
| `palette.rs` | The command palette, grouped by category with a filter line and a block of suggested commands pinned while nothing is typed. Spec: upstream `packages/tui/src/component/command-palette.tsx`. |
| `dropdown.rs` | The inline command menu, anchored above the editor. Opens only when `/` is the first character of the buffer and the cursor has not left the first whitespace-free span; matches descriptions as well as names, which the palette does not. Spec: upstream `packages/tui/src/component/prompt/autocomplete.tsx`. |
| `files.rs` | The inline file menu, raised by an `@`. The same box `dropdown.rs` draws, over paths `ganja-tool`'s `glob` walked in-process — and **not re-ranked**, because upstream says twice in comments to trust the backend's order. |
| `skill_menu.rs` | The inline skill menu, raised by a typed `$` (**D491**, the Codex CLI's selector). The same box again, over `skill::discover` under the engine's own roots — and, unlike `files.rs`, **ranked here** (`nucleo` on the name with the description as a weaker signal, the dropdown's scheme), because there is no backend whose order could disagree. Tab/Enter complete `$name ` without submitting; a non-empty fragment matching nothing closes the menu rather than sitting empty, so `costs $5 each` never parks a box over the prose. Each row's second column opens with its source tag — the installing plugin's name via `plugin::skill_source`, or `(user)` — scored with the description so `$matt` finds a plugin's skills by the plugin's own name. |
| `question.rs` | The question dialog: one question, its options, and — unless the tool said `custom: false` — upstream's last-row free-text entry, opened with Enter, submitted as the answer when non-empty, abandoned back to the options when empty. Esc inside the editor cancels the edit; outside it rejects the question. A batch answers its first question only. |
| `effort.rs` | The `/effort` picker: "Default" first, then the active model's catalog effort names; a selection sends `SwitchEffort`, and the status bar's `model (effort)` segment follows `EffortChanged`. |
| `help.rs` | The reference card: every command with its key, then the bindings no command row shows, named as a config file rebinds them. Sizes itself to its content and **scrolls** when the window cannot hold it — upstream's card is one sentence and never needed to (deviation: `help-card-scrolls`); the footer counts which rows are showing, so a clip is never silent. |

## For AI Agents

### Working In This Directory

- **The transcript is built from engine events alone — the frontend never invents an entry.** The same event stream must replay into the same screen; that property is what P4's resumed sessions and P7's remote clients depend on. If the transcript needs information the engine does not send, the fix is an event in `ganja-core`.
- **The app decides what a keystroke means before the widget sees it.** Enter's meaning (submit vs newline) is resolved in `App::handle`, not inside the editor; the permission dialog's keys are intercepted while it is open. Components render and report — they do not own key semantics.
- **The chat viewport measures its own wrap widths** with `unicode-width` rather than leaning on `Paragraph`, because scroll positions have to agree with what was actually drawn. A change to wrapping is a change to scrolling; keep them in one place.
- The permission modal is centered and must fit in small terminals; the pty suite depends on it landing below the transcript's last line in a tall window (see `../../../ganja-cli/tests/AGENTS.md` for why that matters).

### Testing Requirements

Components are exercised through `App::handle` and rendered into a `TestBackend` — no terminal, no running turn. Screen output is asserted with `insta` snapshots in `../snapshots/`. After an intentional visual change: `cargo insta review`.

Snapshot coverage today: the permission dialog open and overflowing (the cut flagged, the reply keys kept), the sessions picker open and after moving the selection, a tool call in each of its states (pending, running, completed with a diff, error), the theme list open, one style-aware frame per ported theme, the palette open and filtered plus one style-aware frame pinning its selection fill, the inline command menu, the agent list, the reference card, a transcript with a revert marker in place of the messages an `/undo` hid, the history search modal open, the queue strip holding waiting entries, the rewind picker at both its checkpoint and scope steps, the Ctrl+T inspector open (full-terminal takeover, no composer or status bar visible beneath it), the `/mcp` dialog open, the `/team` dialog at both its members step and its action menu, and a teammate's message in the transcript (`component/chat.rs`'s own snapshot). A new tool state or dialog needs its own snapshot.

### Common Patterns

Each component owns its own state struct and exposes methods the app calls (`scroll_pages`, `follow_tail`, …); none reaches back into the engine. Styling goes through `crate::theme` rather than literal colors — themes are loadable data now, and that discipline is why a palette switch needs no component changes. The one exception it forced is explicit: the editor bakes styles into its `TextArea` at construction, so `Editor::restyle` must be called after any theme change.

## Dependencies

### Internal

`crate::theme`, `crate::event::AppEvent`, and the `ganja_protocol` types each pane renders (`PartBody`, `ToolState`, `Usage`, `PermissionReply`, `Role`, `RevertScope`, `MessageId`).

### External

`ratatui`, `ratatui-textarea`, `unicode-width`, `nucleo-matcher` (fuzzy filtering behind the history search modal, the same matcher the palette's own narrowing uses), `serde_json` (tool metadata).

<!-- MANUAL: -->
