# Whether agy's own TUI accepts the one floor that carries over from the
# headless launch line, and what its footer says once the composer is up — the
# recording **D512**'s pane-mode spelling is compared against.
#
# This file is the *recording* the `tui_argv` and `READY_MARKER` tests beside
# `src/teammate/agy.rs` compare against, rather than a second string literal:
# two literals agreeing proves only that somebody typed carefully. Everything
# after `launch: ` is the argv the pane ran, binary first. The headless-only
# flags — `--input-format`, `--output-format`, `--print-timeout`,
# `--disable-slash-commands`, and `-p` itself — do NOT carry to TUI mode and
# are deliberately absent. Everything after `footer marker: ` is the string a
# readiness poll looks for.
#
# Operator: P28 lead (Claude), probes run live; recorded by the W2 lane
# Full record: .omc/plans/2026-08-20-shim-native-tui-panes.md, "Measured facts"
#
# Binary:  agy 1.1.16 (Antigravity CLI)
# Host:    tmux next-3.8, macOS, 2026-08-20
#
launch: agy --sandbox
outcome: composer reached, flag accepted; TUI opens in accept-edits mode ("Accept-edits mode: file edits auto-approved (shift+tab to cycle)")
footer marker: ? for shortcuts
paste probe delivered two lines unsubmitted:
> agy paste probe line one
  line two stays put
enter probe: Enter after the paste submitted BOTH lines as one user message and a real turn ran
hup probe: the agy process survived tmux kill-server (SIGHUP); explicit SIGTERM ended it
