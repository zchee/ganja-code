<!-- Parent: ../AGENTS.md -->
<!-- Generated: 2026-08-04 | Updated: 2026-08-10 -->

# ganja-core/tests

## Purpose

Integration suites for behavior that spans modules, touches a real socket or a real filesystem, or has to be observed from outside the crate. Each file is its own test binary; several deliberately hold exactly one test.

## Key Files

| File | Description |
|------|-------------|
| `agent_loop.rs` | The loop end to end: a turn spans as many model requests as its tool calls demand, every call is gated, executed and answered in order, and the event stream tells the whole story. Providers and tools are scripted doubles. |
| `cancel.rs` | A cancelled turn stops promptly and stays stopped. |
| `cancel_process_group.rs` | A cancelled turn takes the whole process group of the command it was running with it. |
| `delivery.rs` | The lossless guarantee: a consumer slower than the producer still sees every event, in order. |
| `permissions.rs` | Rules from a working directory to a file and back — resolve the project, find its data directory, store an answer, see a later session honour it. |
| `persistence.rs` | A conversation outlives the process: write-through as it streams, resume with interrupted calls closed, auto-title, compaction at the context ceiling. The write-through claim is checked from *inside* an open turn — a provider that is held mid-request is what makes that moment reachable — because a store read after the turn ends cannot tell one that writes as it goes from one that writes at the end. |
| `http.rs` | Both HTTP providers against a real loopback socket: the request actually built, the retry actually scheduled, the body actually split into frames. |
| `golden.rs` | **The differential harness.** Drives ganja *and* real upstream opencode against one replay endpoint and compares the tool calls each executed. See below. |
| `secrets_env.rs` | A canary key planted in the environment must not come back out through a `Debug`, a `tracing` field, or an error body the provider echoed. One test, one binary. The drill runs a third turn through a **config-declared** endpoint, refused by a socket that quotes the key back, because a provider a person configured is a request path like any other and the redaction it inherits has to be inherited in fact. |
| `credentials_env.rs` | A refused credential store must say so and say what repairs it — "no credential" and "there is one and it was refused" are different situations with different fixes. |
| `oauth_store.rs` | `auth.json` is shared territory: a save must not cost somebody a login. One file holding an API key, an OAuth record carrying fields this build has never heard of, and a credential type nobody has invented yet — all three survive a save of an unrelated provider, and storing back the OAuth record puts every field back. Mutates `XDG_DATA_HOME` — one test, one binary. |
| `oauth_store_refresh.rs` | An expiring token is renewed once however many callers notice at once, and not at all while it is still good. Time is paused, so the fake renewal parks on a timer and every caller is inside the window when the count is taken; with the coalescing removed this counts one renewal per caller. Mutates `XDG_DATA_HOME` — one test, one binary. |
| `oauth_grok_device.rs` | A headless xAI login end to end: a login that was abandoned *and* a login that succeeded both leave no `auth.json` at all, because storing is the caller's step and the flow has no store in its reach. Then the other half — the caller stores it under upstream's `xai` key, a spent credential is renewed through the whole public chain (`Refresher` → `grok::Refresh` → disk), and the rotated token is what lands. Drives its own `auth::device::Clock`, so the poll's eight-second wait costs the suite nothing. Mutates `XDG_DATA_HOME` — one test, one binary. |
| `oauth_openai_login.rs` | A ChatGPT login against a real socket: a mock issuer on loopback, the browser flow's redirect answered and its exchange checked pair by pair (the verifier presented is the one the published challenge is the S256 of), the device flow's `403`/`404` polling and the **server-minted** verifier it exchanges, both bounds on that loop, rotation with and without a new refresh token, refused-versus-unreachable renewal, and the `openai` key hazard — a login replaces a stored API key and back. Mutates `XDG_DATA_HOME` — one test, one binary, told in named phases so a failure still says which sentence broke. |
| `oauth_copilot_device.rs` | A GitHub Copilot enterprise login end to end: the **JSON** bodies its endpoints want, a pending poll answered `200` the way GitHub answers it, and the record that results — one token in both fields, `expires: 0`, the deployment beside it. `expires: 0` still reads as *never* after a round trip through the file, at every clock including `u64::MAX`, which is the trap this provider carries: nothing implements a refresh for it, so a credential that ever reported itself due would be due forever. Mutates `XDG_DATA_HOME` — one test, one binary. |
| `oauth_wire.rs` | An OAuth credential *becoming a request*, where the login suites above stop at the credential. One loopback socket serves both halves of a grok turn on different paths, so "how many times was the token endpoint asked" is a count rather than an inference: a live credential travels as a bearer token with no `x-api-key` and costs zero renewals; a spent one is renewed **before** the request is built and the *new* token is what goes on the wire; eight concurrent callers meeting the same expiry renew **once**; `expires: 0` costs zero renewals across many requests; a refused renewal is `Auth` and **not** retryable while an unreachable token endpoint is `Transport` and **is**; and a provider that quotes the access token back in a 401 gets it masked. Mutates `XDG_DATA_HOME` — one test, one binary, told in phases so a failure still says which sentence broke. |
| `fake_script_env.rs` | `GANJA_FAKE_SCRIPT` is read on both routes a session takes to a provider, and *not* on the route the rest of the suite takes. One test, one binary. |
| `live.rs` | Opt-in vendor smoke tests: the request this build sends is one the vendor still accepts today. |
| `live_agent.rs` | Opt-in: one live turn through the real agent loop — a real model, offered this build's real tools, calls them and the arguments parse. |
| `config.rs` | The five-tier precedence table: global file < explicit file < project file < environment < flags, each tier proven to outrank the one below. Mutates environment variables — one test, one binary. |
| `agents.rs` | Agents at the engine level: the planning agent's refusals, config rules deciding unasked, prompt swap on switch, plan reminders reaching requests and not history, switch persistence across a resume, mid-turn refusal, an agent's model adopted from the `provider/model` spelling a config writes, and a passthrough between the switch and the next prompt not spending the build-switch notice. |
| `base_prompt.rs` | The **base** half of the system prompt across a model that moves, composed through the real `instruction::base_prompt`: a cross-family `SwitchModel` and an agent that brings another family's model each land the new family's prompt on the next request, a `default_agent` that prefers another family decides the *launch* prompt under either chaining order, a subagent is handed the family in force rather than the one the parent launched on, a switch within one family changes nothing, and an engine that never asked to follow the model keeps its literal base. The suffix half's own recomposition lives in `agents.rs`; the two are separate binaries so a regression names the half it is in. |
| `permission_directories.rs` | A shell call naming a directory outside the project surfaces that directory in the permission event. Mutates `XDG_DATA_HOME` — one test, one binary. |
| `task.rs` | The task tool end to end: one ordered script drives the parent *and* the child, the child's transcript stays off the frontend's stream, its progress arrives as metadata on the parent's tool part, its registry has no task tool, a parent's denial reaches it while a parent's stored "always" does not, a `task_id` naming a root session starts a fresh child, and a delegated child is stored as its own session naming its parent. |
| `task_tool.rs` | The task tool's own half, driven through a second implementation of the `Subagents` seam (`ganja_testkit::ScriptedSubagents`) with no engine behind it: the model's arguments reach the seam as written, a finished delegation becomes upstream's XML plus the part's metadata, and `Unknown`/`Cancelled`/`Failed` become the three things the model reads instead. |
| `plan_exit.rs` | The plan-exit accept end to end, on a scripted plan turn: a Yes lands the switch at the turn boundary — `MessageFinished`, then exactly one `agent_changed`, then the next turn admitted as build carrying the approval sentence on exactly one asking request — while a No leaves the session planning; an edit in the asking turn is still the plan agent's, a cancel after the Yes still switches, a shell turn keeps the sentence riding, a model switch does not clobber the row, a manual switch supersedes the approval, a restart resumes as build without the sentence, and the tool survives the MCP-dial rebuild. |
| `plan_exit_undo.rs` | An `/undo` after a Yes takes the approval back with the plan it approved — cell cleared, row re-asserted — and a redo does not resurrect it. Mutates `XDG_DATA_HOME` — one test, one binary. Needs `git`. |
| `credential_guard.rs` | A live engine hands its tools the credential store it resolved, so a model that asks `read` for `auth.json` is refused and the planted canary never reaches the transcript. Mutates `XDG_DATA_HOME` — one test, one binary. |
| `mcp.rs` | MCP end to end against a **reference** `@modelcontextprotocol/sdk` server (not an rmcp one): the stdio round-trip, the namespaced names, the permission gate (ask → once, ask → always, a config `deny` wildcard), `isError`, structuredContent-only, binary omission, a server that dies mid-session losing its tools, the `<mcp_instructions>` block, the remote streamable-HTTP transport over a loopback socket, a server that announces a changed tool set moving what the *next* turn is offered (`tools/list_changed`, fired by a fixture that adds one tool and drops another in the same call), a dropped session leaving no server process behind (a stubborn fixture EOF cannot end), and a check that no golden fixture has gained MCP anything. |
| `lsp.rs` | The diagnostics accept: an edit that introduces a type error comes back with rust-analyzer's complaint attached, inside three seconds. Gated on a readiness signal (a pre-seeded error the fixture ships) so the timed edit measures diagnostics latency and not server startup. The 3s budget is the default a development machine is held to; `GANJA_LSP_EDIT_BUDGET_MS` loosens it where scheduling is not the drill's to control — CI sets 6s, just past the client's own 5s ceiling, at which the client stops waiting and the missing diagnostics block fails the run regardless; the margin covers only the drill's own bookkeeping around the call. |
| `steering.rs` | The steering accept (**F4**): a message typed while a turn runs joins that turn at its next step boundary — after a tool step, and again before the turn would otherwise finish, so a message arriving during the model's last request continues the turn instead of ending it (`Event::SteerConsumed` immediately before its own `MessageStarted`; two waiting at once drain in arrival order); a cancelled turn drains nothing; a steer against nothing streaming is refused `NotStreaming` rather than promoted to a turn of its own; a steer's mentions resolve when the boundary drains it, not when it was typed; a steered message sorts after the reply it interrupted and replays identically on resume; and steering leaves `EngineError::Busy` exactly where it was for a second `SendPrompt`. A `GatedProvider` holds a request open to land a steer inside the model's *last* request — the one seam a scripted stream can't otherwise reach. |
| `undo.rs` | The undo accept: a scripted turn edits a tracked file and writes a new one in a real temp checkout, `/undo` restores the first byte for byte and removes the second, `/redo` puts both back, and the prompt sent after a second undo reaches the model carrying **no** trace of the one it replaced — captured off the fake provider, because the worktree comparison alone is blind to history. Mutates `XDG_DATA_HOME` — one test, one binary. Needs `git`. |
| `rewind.rs` | The rewind accept (**F7**): `Command::RevertTo` against a real temp checkout, once per scope — `Both` restores the checkpoint's files and hides its messages (redo puts them back, the next prompt makes it permanent), `Conversation` moves the transcript with the working tree untouched, and `Files` moves the working tree while hiding nothing, so a `/redo` after it finds nothing and an `/undo` still anchors on the message whose files just came back. Plus the honesty case: a patch naming a path the checkout cannot restore is left out of what the event reports (unix-only, and it checks its own premise — root is not subject to the permission bit it rests on). The scope drill mutates `XDG_DATA_HOME`; the two refusal tests beside it take no snapshots and touch no stored state. Needs `git`. |
| `passthrough.rs` | `!` passthrough: the exact synthetic user text, the `bash` part completing with the output and no exit code, ungated even where a rule refuses the model, running at the project root rather than the process directory, and a cancel that stops the command. |
| `commands.rs` | Slash commands, compaction on demand, and starting over: `/init` writing `AGENTS.md` through the ordinary loop, argument expansion reaching the prompt, a configured command, and `NewSession` leaving the old session on disk. |
| `command_rules.rs` | What a frontend refuses for the whole session, against everything that re-derives a baseline beneath it: a `/command` naming its own agent is still bound by a standing refusal the agent itself allows, the command agent's *own* rules still decide inside that derived turn, an agent switch does not drop the refusal (the same door an MCP server's background dial opens), rules imposed before `with_agents` survive it, and a tool no standing rule names still runs. `question` is the permission throughout, because it is the one `ganja run` refuses first and the one ganja registers next. |
| `compat_anthropic_wire.rs` | A config-declared Messages-dialect endpoint becoming a whole turn, and the half its sibling cannot prove: `x-api-key` rather than a bearer, `/v1/messages`, and the dated `anthropic-version` travelling to an endpoint that is not Anthropic's — with the *builtin's* key exported to a wrong value, so a wire reading the vendor's variable instead of the entry's fails loudly. Mutates environment variables — one test, one binary. |
| `compat_openai_wire.rs` | A config-declared chat-completions endpoint becoming a whole turn through the ordinary engine: the request asserted whole against a loopback socket — the entry's path, the bearer from the variable the entry named, the entry's declared header, and eight builtin-provider headers asserted **absent**. Mutates environment variables — one test, one binary. |
| `compat_selection.rs` | `GANJA_PROVIDER` naming a config-declared endpoint — the third route to one, and the only one needing the environment — plus the refusal for a name nothing has, which must list the builtins *and* this config's own entries, and the startup refusal when neither the named variable nor the store holds its key. Mutates environment variables — one test, one binary. |
| `compat_uncataloged.rs` | What a session on a provider no catalog knows gives up, pinned by name: a session seeded past every threshold takes **one** request rather than two (no summarize), no summary opens the stream, and the tokens are counted while nothing can price them. Mutates environment variables — one test, one binary. |
| `watcher.rs` | The stale-read watcher against a real `notify` watch over a real directory: a file edited from outside is named to the model and refused afterwards even once its stamp is put back (so what refuses is the watcher's verdict and not a comparison), a tool's own writes are not condemned by the events they cause, and a subtree the session never reads is never watched at all. Every test fences on the watched-set accessor before changing anything — registration is lazy and stats what it registers, so a change made before the watch existed would be caught by that stat rather than by an event, and the drill would pass on a platform where watching does nothing. |
| `mentions.rs` | `@file`: the reference on the message, the content in the request, read at **send** time (a file rewritten between two turns reaches the model rewritten), and never recorded as a read. Now also the ranged and binary halves: `#2-4` inlines exactly those lines, a png rides as base64 with its real mime, and the same png on a wire that refuses attachments reaches the model as text naming the file and the mime. |
| `command_expansion.rs` | Command-template expansion crossing the engine: a `` !`cmd` `` template reaching the model as the command's output, a non-zero exit substituting anyway, a template `@file` attaching while a name that resolves to nothing stays literal, and expansion running at the project root rather than the process directory. Spawns real shells — own binary. |
| `effort.rs` | Efforts end to end on a canned catalog: a selected effort's option map riding the next request, a model switch to a model without efforts clearing and announcing, a wrong name refused naming the real ones, and the stored effort surviving a resume. Mutates environment variables — own binary. |
| `catalog_fetch.rs` | The catalog against a real socket: the startup loop fetches, the payload parses through fields this build has never heard of, the table is replaced wholesale, the body is cached verbatim under a name derived from the source, and the five-minute debounce keeps a second refresh off the wire. Mutates environment variables — one test, one binary. |
| `catalog_offline.rs` | Fetching disabled and nothing cached: the compiled-in snapshot answers every question, and a loopback listener that would have counted a request counts none. Mutates environment variables — one test, one binary. |
| `catalog_retry.rs` | A refused catalog request is retried twice and then reported as the status it gave up on, and a refresh that failed leaves the table it started with. Mutates environment variables — one test, one binary. |
| `skills.rs` | Skills end to end in four phases, with ganja's own two homes and every directory upstream walks unasked planted side by side: a session that configured nothing reads `<config home>/skills` and `<project root>/.ganja/skills` — offered in `<available_skills>` and loadable — and reads no foreign directory nor the `skill/` spelling beside the one it takes, while the fixture-built engine (no environment half, the registry's own rootless skill tool) is offered nothing at all so a golden run compares two agents rather than two machines; a `skills.paths` entry is read on top of both and ranks above them; `["~/.claude/skills"]` gets upstream's own tier back, opt-in; and the config home itself is moved through all three of its places, proving one seam by watching the global `AGENTS.md` travel with the skills. Mutates `HOME`, `XDG_CONFIG_HOME` and `GANJA_CONFIG_HOME` — one test, one binary. |
| `spill_sweep.rs` | The spill sweep over the directories it really resolves: both candidates a clamp would have written to, week-old `tool_*` gone, fresh and foreign kept. Mutates `XDG_DATA_HOME` and `TMPDIR` — one test, one binary. |
| `grok_startup.rs` | A grok session with no login refuses **at startup**, where every other provider already does and where the message is readable — and the probe that decides it reads no token material: a canary planted in the store reaches neither the refusal, the provider's rendering, nor a `tracing` field. A stored API key counts as a credential, because `NotOauth` is a different sentence and the request is where it belongs. Mutates `XDG_DATA_HOME` — one test, one binary. |
| `copilot_wire.rs` | A GitHub Copilot subscription becoming a turn, against a real socket: a whole streamed chat-completions turn through the ordinary engine (all four spike-measured headers, no `x-api-key`, the body and the bill), the bearer compared byte for byte against the **stored** token rather than a constant — there is no exchange in the pin, so anything that transformed it reddens here — a `finish_reason: "length"` with empty content published as a **completed** turn rather than an auth failure, the endpoint derived from the login's deployment (enterprise and public), the stored token replaced mid-suite so the *second* one is what the second request carries, and the selection: `GANJA_PROVIDER=github-copilot` resolves to this provider and a model the catalog can size, while removing the credential still builds a provider and fails at the request with the message that names the login. Mutates `XDG_DATA_HOME` and `GANJA_PROVIDER` — one test, one binary, told in phases. |
| `reasoning_downgrade.rs` | A stored reasoning part this build cannot read, from the record to the wire: the message comes back whole apart from the state, the loss is *in* the transcript as a stateless reasoning part rather than only in a log line, the request built from it carries no reasoning item and — canary-checked — none of the state the record actually held, and the row is still byte for byte what the future build wrote. Mutates `XDG_DATA_HOME` — one test, one binary. |
| `responses_wire.rs` | An OpenAI session becoming a turn on **either** credential, against a real socket, in ten phases: a whole streamed subscription turn through the ordinary engine (all five headers, the input items, the flat tool shape, the bill with the cached half taken back out); the stored credential rotated mid-suite so the *second* token is what the second request carries; a **key** turn on the same wire at the platform, with the four subscription headers asserted **absent**; the chat-completions body still compared byte for byte, because grok and Copilot ride that encoder even though an `openai` key no longer does; the dispatch (a key wins, a stored login serves); the default each wire hands a session that named no model, and an explicitly named one never substituted; neither credential as the startup error with nothing on the wire; nothing leaking; the seat's allow-list refusing a model **before** a request is spent while the same name takes a whole turn on a key; and, last, thinking surviving the request that produced it: a scripted turn whose first reply seals a thought and calls a tool, with the **second** request's `input` carrying that state back in upstream's item shape. Mutates `XDG_DATA_HOME`, `OPENAI_API_KEY`, `OPENAI_BASE_URL`, `GANJA_PROVIDER` and `GANJA_MODEL` — one test, one binary, told in phases. |

## Subdirectories

| Directory | Purpose |
|-----------|---------|
| `fixtures/` | Golden task scripts and the MCP reference servers (see `fixtures/AGENTS.md`). The recorded SSE transcripts these suites replay moved to `ganja-provider/tests/fixtures/`, beside the wires that parse them. |

## For AI Agents

### Working In This Directory

**Know which suites need setup before concluding a failure is a regression.**

- `golden.rs` needs `bun` on `PATH` and an upstream opencode v1.18.13 checkout with `bun install` already run — at `.omc/reference/opencode-v1.18.13` or wherever `GANJA_OPENCODE_DIR` points. A missing checkout or missing `bun` is a **failure, not a skip**: this suite exists to hold the port to upstream's behavior, and a green run that silently compared against nothing would be worth less than no run. Each upstream leg gets 180s.
- `mcp.rs` needs the same `bun` + upstream checkout `golden.rs` does, and additionally that checkout's installed `@modelcontextprotocol/sdk` (resolved at `packages/opencode/node_modules/@modelcontextprotocol/sdk`, with the hoisted copy as fallback). Missing is a **failure, not a skip**, for golden's reason: the whole point is that the server is somebody else's implementation, so a run that talked to nothing would prove nothing.
- `lsp.rs` needs `rust-analyzer` on `PATH` (`rustup component add rust-analyzer`). Missing is a **failure, not a skip**, for golden's reason: a run that started no language server would prove nothing.
- `undo.rs` and `rewind.rs` need `git` on `PATH`, and fail rather than skip without it for the same reason. Both build their own checkout in a temporary directory, so nothing they do depends on the repository they are run from.
- `live.rs` / `live_agent.rs` are `#[ignore]`d *and* inert unless `GANJA_LIVE_TEST=1` plus the provider key are both set:
  ```sh
  GANJA_LIVE_TEST=1 ANTHROPIC_API_KEY=… cargo test -p ganja-core --test live -- --ignored
  ```
  That combination is deliberate: a contributor running the full suite spends nothing, and CI can opt in without failing on a machine with no key.

### Testing Requirements

```sh
cargo nextest run -E 'binary(agent_loop)'
cargo nextest run -E 'binary(golden)' --no-capture
```

### Common Patterns

- **One test per binary where process-wide state is mutated.** A file that sets environment variables or depends on the process working directory (`secrets_env.rs`, `fake_script_env.rs`, `golden.rs`, `credential_guard.rs`, `oauth_wire.rs`) holds exactly one test. nextest runs each test in its own process, so it would tolerate more — but a plain `cargo test` runs a binary's tests on parallel threads, and the one-per-binary rule keeps the suite correct under both runners. Do not add a second test to those files — put it in a new file.
- **Redirect `XDG_DATA_HOME`.** Anything that reads or writes stored state must not touch the real user's credentials, permissions or spilled output.
- **Serve real bytes, don't mock the client.** Provider suites bind a loopback `TcpListener` and speak real HTTP, because mocking would skip the request that is actually built and the frames it is actually split into.
- **Assert on the redacted tail, never a whole key** — a test that printed one would put it in CI output, which is the failure redaction exists to prevent.
- In `golden.rs`, scripts are handed out to *agent* requests only — the ones carrying a `tools` array — because upstream opens a session with an extra toolless title request that would otherwise shift its whole script by one and compare the two legs against different transcripts. Keep any new engine-side bookkeeping request toolless so this discrimination keeps working.

## Dependencies

### Internal

`ganja_core`'s public surface only — these suites consume the crate the way a frontend does.

### External

`tokio` (with `net`), `futures`, `tempfile`, `tracing-subscriber` (the secrets canary needs a subscriber it can read back), `serde_json`, and `bun` as an external binary for `golden.rs` and `mcp.rs`.

<!-- MANUAL: -->
