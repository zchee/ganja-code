<!-- Parent: ../AGENTS.md -->
<!-- Generated: 2026-08-04 | Updated: 2026-08-05 -->

# ganja-core/src

## Purpose

The engine's modules. The shape to hold in mind: `engine.rs` accepts commands and publishes events, `session.rs` runs one turn's agent loop against a `ganja-provider` wire, executing `ganja-tool` calls that `ganja-permission` gates, with `ganja-protocol` supplying every type that crosses the boundary. Those four are separate crates, re-exported by `lib.rs` under the module names they always had — `crate::tool`, `crate::permission`, `crate::project`, `crate::protocol`, `crate::watch`, `crate::auth`, `crate::catalog` all still resolve, and now resolve across a boundary the compiler enforces.

## Key Files

| File | Description |
|------|-------------|
| `lib.rs` | Module list, the facade that re-exports `ganja-protocol`, `ganja-permission`, `ganja-tool`, `ganja-provider` and — since P25 — `ganja-team` (as `::team`, the one that was born rather than split off) under module names, and the crate root's own flat re-exports — the engine's types only; the five crates beneath stay behind their module names. States the no-terminal-dependency rule that CI enforces. |
| `engine.rs` | `Engine`: commands in, an ordered event stream out. Owns the turn lifecycle and the transcript. |
| `session.rs` | The agent loop — one turn, as many model requests as its tool calls demand. Also `TurnHandle`/`PendingReplies` (P13: a map keyed by request id, not the single `Option<PendingReply>` cell it used to be — several children can each hold a dialog open now, one per id), and the four-phase call resolver (`prepare`→`start`→`body`→`finish`) a batch of concurrent `task` calls and an ordinary sequential call both run through, so only `body` ever leaves the turn's own thread. P23 added `skill_parts` (**D491**) at the two seams `mention_parts` extends `user.parts`: a prompt's or steer's `$name` invocations become the `skill` tool's own rendering — `skill::rendered`, shared, byte-identity-pinned by `tests/skill_invocation.rs` — or its not-found sentence, off `Turn::skill_roots`; a miss is information, never a dead turn, and nothing here crosses a permission gate or fires a hook. |
| `hook.rs` | Commands a config runs at nine named moments (`PreToolUse`, `PostToolUse`, `UserPromptSubmit`, `Notification`, `Stop`, `SubagentStop`, `SessionStart`, `SessionEnd`, `PreCompact`), spawned under the `bash` tool's own POSIX shell with the event's JSON envelope on stdin (this module owns its own spawn — `ShellTool`'s hardcodes `Stdio::null()`). Every hook matching an event runs concurrently, awaited under its own timeout; exit 0 passes and its stdout is read as the documented envelope or plain text, exit 2 blocks where blocking means something (`PreToolUse`/`UserPromptSubmit` only), anything else — including a timeout kill — is a non-blocking failure reported through `tracing`, never a new protocol event. Runs with the user's own authority, crosses no permission dialog, and never fires for another hook. No upstream counterpart (**D456**). |
| `job.rs` | Background shell jobs: a `bash` call started with `run_in_background: true` returns immediately and keeps running under `JobRegistry`'s own root cancellation token — a turn's cancel never reaches it. `bash_output` drains a job's output since the caller's last poll (one cursor per job) with an optional regex `filter`; `kill_shell` ends the whole process tree. No upstream counterpart (**D454**); the `timeout` argument is deliberately never consulted on this branch (**D455**). |
| `provider.rs` | **Half a module, and the half that reads a `Config`.** The wires themselves are `ganja-provider`'s, re-exported whole by a glob at the top of this file so `crate::provider::AnthropicProvider` and every other path already written still resolves. What stayed is selection: `select`, the four-tier chain that decides which provider a session runs as and which model it asks; `Selection`/`SelectionError`; `selectable`, the first of the two tiers `PROVIDERS` describes; `Wire` and `openai_provider`, the dispatch that reads a *kind* of credential rather than a provider name and hands back a provider plus the default **that backend** wants; `configured_provider`, the config→wire translation; and `defaulted_model`, the precedence a test can state. The line is exactly `Config`: everything below it takes plain data, which is why the wires could leave and this could not. |
| `auth`, `catalog` | Facade re-exports of `ganja_provider::auth` and `ganja_provider::catalog`, named in `lib.rs`. They moved with the wires that read them — see `../../ganja-provider/src/AGENTS.md` for what each does. |
| `storage.rs` | Session storage: one SQLite database per project, `session`/`message`/`part` with opaque JSON `data` columns, upstream's migration journal and pragma set. Writes go through a dedicated thread so their order is structural; reads take a second connection, which is what WAL buys. Records still carry an explicit version, so one a newer build wrote is left alone and one that will not decode costs its own row and nothing else; a database that cannot be read at all is set aside with its write-ahead log and replaced. A `storage/` tree from a build before this one is carried across on first open and renamed rather than deleted. A part row that will not decode still costs only its own row, with one named exception: a record whose `type` names reasoning is *request-affecting* state, so the message keeps a stateless `PartBody::Reasoning` in its place and a warning says continuity was lost — the record itself untouched, and nothing salvaged out of a shape this build failed to parse. |
| `subagent.rs` | Crate-private. Runs the second agent loop a `task` call delegates to: the per-session `Host`, the per-call `Spawn` that implements `tool::task::Subagents`, the roster a caller may spawn, the child's derived ruleset, its private event channel and the watcher that turns it into progress on the parent's part. **P25** put the messaging seam here too, beside the delegation one: `Postbox` — `tool::team::Postbox`'s implementation, whose sender is bound at construction and is never an argument — plus the cross-session tail (**D505**), where a validated `uds:` address becomes `GET /team` to learn who leads the session at that socket and then `POST /team/{lead}/message` with plain text stamped `<sender>@<team>`, and `receive`, the inbound arm that climbs the tool's own rungs on the side that has no tool in front of it. Nothing structured crosses either way: the tool refuses it at rung 6, the outbound arm refuses it again, and the inbound arm classifies the text before anything is written. |
| `teammate/` | **P25**: agents that outlive the call that started them. `TeammateRegistry` takes `JobRegistry`'s shape for its reason — a cancellation token that is a child of nothing a turn holds, so cancelling a turn never ends a teammate — differing only in what a shutdown *waits* for, since a teammate's turn is settled rather than killed: its transcript is a session somebody may open tomorrow (**D500**). `TeammateBackend` is the one trait, with three implementations chosen by name and refused by name, never silently fallen back to (**D501**): `runner.rs` (in-process, the default), `pane.rs`/`tmux.rs` (a ganja in a tmux pane) and `claude.rs` (a real `claude` binary in one). `pane.rs` mints **D502** — a pane inherits an *enumerated* environment, and its launch line is ≥2 words on purpose, because tmux runs a one-word command through `$SHELL -c`, which sources rc files and re-imports exactly the credentials the enumeration left out (measured: `ANTHROPIC_API_KEY` reappeared via `.zshenv`). `reaper.rs` mints **D506**: a recorded pane is ended on what it is *running* — argv carrying both `--agent-id <id>` and this lead's `--parent-session-id` as flag/value pairs — never on what was recorded, because a pane id is recycled. `posture.rs` decides what a teammate does with a dialog it raises — forwarded to the lead by default, allowed outright only where a person asked for that at spawn. `member.rs` is the other side: what a process that *is* a teammate holds, its postbox over the team file read fresh each time, and `Asks`, the pane's `ForwardToLead` over §5's two permission frames — driven by the frontend, never spawned, because two readers of one inbox would race. `lead_inbox.rs` is the lead's §6.2 pass, a UI hook rather than a service: it reads, classifies and prunes and hands the frontend what to act on, which is why the tick owns the cadence and this module owns none. |
| `config.rs` | `ganja.jsonc`/`ganja.json`: discovery (global dir, `GANJA_CONFIG`, project walk-up), JSONC decode of the curated keys, tier merge with flag overrides on top. Unknown top-level keys are refused by name; permission rule order survives exactly as written. The `provider` table declares endpoints this build does not ship — id, `dialect`, `base_url`, `key_env`, `headers` — refusing per entry an unknown field, an id a builtin already has, an endpoint that would carry the credential in the clear, and a blank `key_env`. Also `default_skill_dirs`: ganja's own two skill homes (`<config home>/skills`, `<project root>/.ganja/skills`), resolved through the same seams config discovery itself uses, because the crate that knows where ganja keeps its things is the one that should say so. And `config_home`, the one place ganja's own directory is resolved — `$GANJA_CONFIG_HOME`, else `$XDG_CONFIG_HOME/ganja` if it exists, else `~/.ganja` if that does, else the XDG path as what a writer should create — read by the global config tier, the global `AGENTS.md`, `default_skill_dirs`, the TUI's theme directory and the importer's `--global` destination alike, so none of them can come out of a different directory. **P13** added `hooks` (Claude's own `{matcher, hooks:[{type:"command", command, timeout}]}` shape, keyed by event name, checked by `check_hooks` beside `check_mcp`) and `agents.concurrency` (`AgentsConfig`, its own settings object beside the `agent` name map, default four, refused at zero) — both curated and refused unknown by name like everything else here. **P17** added `effort`, the catalog effort a fresh session starts under: carried unvalidated because which names exist depends on the model the tiers settle on, and adopted (or cleared) by `Engine::seed_effort` rather than checked here. |
| `plugin.rs` | Claude Code's `.claude-plugin` plugin and marketplace shapes read verbatim (**D472**), over an install store at `<config home>/plugins/` whose only doors are the typed `ganja plugin` subcommands and the `/plugin` dialog — install is the consent, because a plugin's hooks and MCP servers run with the user's own authority. Foreign manifests are read tolerantly (no `deny_unknown_fields`, the opposite of `config.rs`) but every name is refused at parse if it could escape the store, and an add stages → validates → moves so no failure leaves half of one behind. One `collect` walks a plugin's components for the listing and the load path both; `apply` merges them per surface at the single point in `Config::load_with` (**D473**), because `Config::merge`'s per-event-key replacement would silently kill every plugin hook. Six surfaces: hooks append, MCP arrives namespaced `plugin:<name>:<server>`, skills roots concatenate, `commands/*.md` join the `command` table as `<plugin>:<name>` through `command.rs`'s own file loader rather than a second parser (**P22**, the sixth and last), and agents and LSP merge per key with the explicit config winning and the collision warned by name. A component file that does not parse is skipped with a warning; ganja's own `plugins.json` that does not is a hard error. |
| `mcp.rs` | MCP servers and the tools they lend: config-named servers dialled concurrently in a background task, `connected \| disabled \| failed{error}` per server. Tools are named `mcp__<server>__<tool>` with upstream's sanitizer on each half, contributed in sorted-server / listed-tool order with a post-sanitization collision refusing the later tool. Schemas are forced to `{type:"object", properties, additionalProperties:false}`; `isError` becomes error text the model reads; a server's `instructions` reach the system prompt as `<mcp_instructions>`. Prompts and resources are still not ported. **P13** retired the "no reconnect" posture: `Servers::reconnect` (manual, `/mcp` dialog) and `Servers::retry_once` (automatic, once per server per session) both re-dial through the existing `connect` (**D463**); a result over a server's `output_limit` clamps through `truncate::clamp_bytes` (**D464**); a remote entry's `oauth: {}` drives RFC 8414 discovery + RFC 7591 registration + PKCE, storing under a reserved `mcp:<server>` credential-store key, sending the bearer proactively and refreshing-then-redialling once on a `401` (**D466**). |
| `lsp/` | Language servers and the diagnostics the model reads. Opt-in: no `lsp` key means none ever starts. A server is spawned lazily by the first touch of a file it claims, identified by `(root, server-id)`, and a pair that fails to start is never retried this session. Diagnostics arrive on both channels — pushed publishes and `textDocument/diagnostic` pulls — merged and deduped, because rust-analyzer stops pushing for a file edited after its initial analysis. Errors only, capped at 20 per file, appended to the tool result at one seam in `session.rs`. Two builtins ship, `rust` and `gopls`; nothing is ever auto-installed. |
| `snapshot.rs` | Git snapshots of the working tree, and the `/undo` and `/rewind` they make possible. A **separate git dir** under the data home, every command `git --git-dir <ours> --work-tree <the project>`; the project's own `.git` is only ever read. No commits — `write-tree` names a tree and that hash is the snapshot. A turn records one `PartBody::Patch{hash, files}` per step that changed files, naming the tree the step **started** from, and reverting is `checkout <hash> -- <file>` per file with a file absent from that tree deleted instead; `Snapshots::revert` reports the **achieved** list — what really came back — which `Engine::revert_to_message` (any user-message anchor, `RevertScope::Both`/`Conversation`/`Files`) hands to its event, while `Engine::undo`/`redo` keep reporting the intended list (a recorded follow-up). Also the walk those three drive: the anchor, the patches from it on, and the prompt it hands back. Off when there is no `git`, when the directory is not a checkout, or when `snapshot: false`. |
| `command.rs` | Slash commands: the builtin `/init` plus whatever `config.command` describes, and the expansion that turns one into a prompt — `$1`/`$ARGUMENTS` fill, then every `` !`cmd` `` runs through the passthrough's own spawn at the project root and substitutes its output in match order (failures included), then the template's `@file` mentions attach the way the composer's do, under a scan mirrored from the TUI's and pinned equal to it. **P16** added the file tier (**D481**): flat `*.md` command files from the two homes, frontmatter over body-as-template, layered beneath the config's own declarations. **P22** made that loader `pub(crate)` so an installed plugin's `commands/` reads through it — one file format, one parser — and added the load-time refusal a file's `agent:` earns when it names nobody (dropped, file and agent logged; the config table's own dispatch-time `UnknownAgent` is untouched), applied by the engine because that is the first place both rosters exist. |
| `attachment.rs` | Upstream's mime table for `@` attachments: which extensions attach, svg reads as text, everything else as bytes, an unknown extension stays `text/plain`. The table only says what attaches; whether a mime survives the wire is the provider's question, asked at request build and again at the composer's submit. |
| `instruction.rs` | The system prompt: a base prompt per model family (`prompt/*.txt`), the `<env>` block, and `AGENTS.md`-family instruction discovery, assembled in upstream's `Instructions from:` shape. Reaches the engine through `Engine::with_system`/`with_system_parts` — and both halves are written against a model, so the engine recomposes each whenever the active one moves: `Engine::with_base_for_model` calls `base_prompt` again (the family's prompt), `Engine::with_environment` calls the suffix composer again (the block that names the model). P15 added two more composition members: the lazy nested `AGENTS.md` walk-in a touched file's parent chain earns (**D480**) and the opt-in per-project memory section with its synthesized upkeep block (**D478**). |
| `agent.rs` | The agent roster: build, plan, general, explore (upstream's rulesets adapted to ganja's tool surface), config overlay, `default_agent` resolution. An agent is a name, a prompt that replaces the base prompt, and rules layered beneath the user's stored answers. The attended agents alone carry the memory-root door (**D478**) — a subagent's baseline is its own agent's rules and inheritance passes refusals, never allows, so a child's memory write still asks. **P16** added the agent file tier (**D482**): `agents/*.md` from the two homes, body as prompt, `tools:` compiled to a refused-not-hidden permission wall that never touches the conversation tools. |
| `prompt/` | Upstream prompt texts, byte-verbatim — attributed in the root notices. |

## For AI Agents

### Working In This Directory

The invariants below are what the tests actually pin. Breaking one is a behavioral regression even if everything compiles.

- **Delivery is per-subscriber, and each subscriber chose its policy.** Every subscriber has a bounded queue of its own (`engine.rs`, `Fanout`). A lossless subscriber (`subscribe()`) is never dropped: its full queue makes the publisher wait, so backpressure lands on the turn task and never on a render loop. A droppable subscriber (`subscribe_droppable()`) is never waited for: a full queue evicts it whole, and its stream ends with `Evicted` — an observable error, never a silent gap. Do not swap in `broadcast` — lag-drops would silently tear a lossless subscriber's transcript.
- **The first subscriber inherits the birth queue; later ones join from registration.** The engine buffers into its birth queue from construction, which is what lets `ganja run` and the TUI resume — emitting `RevertChanged` — before anything subscribes. Every later `subscribe()` sees events from the moment it returns: registration is the atomic point after which nothing published is lost, so two lossless subscribers of one turn hold the same transcript frame for frame. One turn at a time is unchanged: a prompt sent while a turn is streaming — or waiting on a permission — returns `Busy`.
- **The event stream is the whole story.** A frontend that applies every event holds exactly what the next `ChatRequest` will carry. Anything the engine knows and does not report is a bug in the protocol, not something a frontend should reconstruct.
- **Tool results are information, never control flow.** A refused permission, an unknown tool, unparseable arguments, a tool that failed — each becomes error text the model reads on the next request, and the loop continues. (This is a deliberate divergence: upstream stops the turn on a refusal unless `experimental.continue_loop_on_deny` is set.) The only two early exits are the ones that mean it: the user cancelled, or the provider died. There is no step cap — `Command::CancelTurn` is the escape hatch, as upstream.
- **A turn always ends with a terminal event.** The turn task is deliberately never joined and never aborted from outside: cancellation reaches the provider and every running tool, and aborting the task instead would skip the cleanup that releases the busy slot and guarantees the finish event.
- **Failed turns do not enter the history.** An empty reply is dropped rather than carried into the next request.
- **An MCP tool asks by default.** `ganja-permission` decides it by **prefix** (`MCP_PREFIX`, whose one owner it is), not by name, because the names are not known until a server has been asked. Do not fold this into `ASK_BY_DEFAULT` — that list is names, and this is a shape.
- **The tool surface moves only between turns.** `Engine::refresh_mcp` runs at the `start_turn` seam; a turn already holding a registry snapshot keeps the tools it started with, so a connect finishing mid-request cannot change what that request was answered with.
- **No LSP failure may fail a tool call or a turn.** A server that will not start, a file that will not read, a publish that never comes — each costs the model a diagnostics block and costs the turn nothing. The whole subsystem's output is advice.
- **Diagnostics reach the model at one seam.** `session.rs::resolve` appends them after `tool.run` and before `ToolState::Completed`, keyed by tool id — not inside `edit`, `write` and `read`. Adding a fourth tool that wants them is an edit to that list, not to the tool.
- **A reminder belongs to the request, never to the transcript.** The plan/build notices and the stale-files notice are appended to the last user message of the `ChatRequest` a turn builds and are not written through: each is about the state the session is in *now*, and a stored copy would still be telling a later turn about a mode it left or a file it has long since re-read.
- **No watcher failure may fail anything, and a passthrough may not spend a notice.** The stale queue is drained only by a turn that asks the model — a `!` command asks nothing and a compaction asks a question of its own — so the notice waits for the prompt that can actually deliver it.
- **Nothing on a startup path may register a watch.** `Engine::watch_files()` touches no filesystem: the platform watcher is built, and every directory registered, on the watcher's own task. Registering a *recursive* watch on Linux is a synchronous walk of the whole tree — `notify`'s inotify backend blocks the caller while it walks and spends a descriptor per directory — which is why registration follows the read log one directory at a time and why a future background service should be assumed to cost what its worst tree costs, not what it costs here.
- **No snapshot failure may fail a tool call or a turn.** Every git invocation answers with an exit code rather than an error; a step whose snapshot did not happen simply records no patch, and the loop never asks whether one did.
- **A patch names the tree its step started from.** Restoring is checking those files out of that hash, so the direction is not reversible by taste: the hash a step records is *before* it ran, and the step-finish work only decides whether anything moved.
- **Sealed reasoning is written through and published like any other part.** The next request is built from the transcript, so state that lived only in the step loop would be state the turn's second step no longer has; and a frontend that applies every event must hold exactly what the next request will carry. Nothing renders it, and nothing may read it.
- **A revert deletes nothing.** `Command::Undo` hides messages by recording an anchor; they stay in the history and in storage, which is what makes `Command::Redo` lossless. Deletion happens once, at the next `SendPrompt` or `RunShell`, and it takes the anchor **with** everything after it — a prompt the user took back must not reach the request that replaces it.
- **Root turns stay serial; only `task` children fan out.** `TurnSlot`/`Busy`/the one-turn-at-a-time rule above are unchanged by P13 — concurrency lives strictly *inside* a running turn (consecutive `task` calls) or *beside* turns (background jobs), never across two root prompts.
- **A hook runs with the user's own authority and crosses no permission dialog, and never fires for another hook.** The trust boundary is authorship of the config file, the same as a command template's; a hook that writes files no dialog approved is a documented risk, not a bug. Nothing here may turn a killed hook into `allowed` — the only route there is an explicit `permissionDecision:"allow"` on a clean exit.
- **A background job never rides a turn's fanout.** `job.rs`'s registry owns its own root cancellation token; a turn's cancel reaches every tool call that turn started and nothing a `run_in_background: true` call handed off.

### Testing Requirements

Unit tests sit in `#[cfg(test)] mod tests` at the bottom of each module and cover the module's own logic. Cross-module behavior — the loop, cancellation, delivery, permissions round-tripping to disk — belongs in `../tests/`. When adding an invariant to this directory, add the test that would notice if someone removed it.

### Common Patterns

- **Ids sort in creation order.** `MessageId`/`PartId`/`PermissionId`/`SessionId` are a millisecond timestamp plus a per-process counter, both fixed-width hex, so ids sort lexicographically by creation and cannot collide in one process. Storage reassembly (P4) leans on this.
- **Every event names its session, filled from one slot.** The engine holds a single current-session id: minted at construction (an ephemeral engine's session has a name with no row), adopted by the first prompt's lazy create so turn-1 events and the stored row agree, replaced by `resume` before the resumed revert is announced, and re-minted by `NewSession` so the next conversation cannot upsert over the last one's row. A subagent's crossing permission dialogs are re-addressed to the parent's session — the child session is invisible to every frontend.
- **Ids adopted from disk are taken verbatim.** The prefix is a convention, not an invariant.
- New `PartBody` variants are additive — the tag travels as a `type` field beside the part's id — so adding one changes nothing already on the wire; `ReasoningText` proved it and `ServerTool` (D489) repeated it.
- Time is milliseconds since the Unix epoch via `protocol::now()`, saturating rather than failing when the clock is set before 1970.

## Dependencies

### Internal

`ganja-protocol`, `ganja-permission`, `ganja-tool` and `ganja-provider`, re-exported by `lib.rs` so that `crate::protocol`, `crate::permission`, `crate::project`, `crate::tool`, `crate::watch`, `crate::auth` and `crate::catalog` mean here what they have always meant. The flow is `engine` → `session` → {`provider`, `tool`, `permission`}, with `protocol` beneath all of them and `project` supplying the data directory that `permission` and `storage` write into. The direction is one-way and the compiler enforces it: none of the four may name this crate. `crate::provider` is the one that is not a bare re-export — a glob over `ganja_provider::provider` plus the selection half that stayed, because that half reads a `Config` and the wires do not.

### External

See `../AGENTS.md`.

<!-- MANUAL: -->
