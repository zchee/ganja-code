<!-- Parent: ../AGENTS.md -->
<!-- Generated: 2026-08-28 -->

# ganja-teammate-local

## Purpose

This crate's `tmux.rs` is **not** the sealed-leaf `tmux` workspace crate — it is this workspace's own handful of `tmux` subprocess calls with the pane-identity rules P25b needs. It is not consumed by, and does not consume, the sealed leaf; CI's inverted gates over `tmux`'s own tree and over every other member's tree (`crates/AGENTS.md`) prove the two never touch, in either direction, and never will by accident.

The teammate backends that can only run on the machine the lead is running on: tmux panes, the `ganja` and `claude` panes split into them, and the three foreign CLIs — `codex`, `grok`, `agy` — driven in their own native TUIs. `ganja_core::teammate::TeammateBackend` is a seam with more than one adapter, which is what makes it a cut worth making (**D538**, **D539**; `.omc/plans/2026-08-28-teammate-seam-crate-split.md`) — every adapter here needs a tmux server, a shell to split into, or somebody else's binary on `PATH`, none of which an engine may hold. So this crate sits **above** `ganja-core` the way a frontend does: it names the engine, and the engine names nothing here, both directions asserted in CI rather than trusted — this crate's own internal dependency list is a closed allowlist, and `ganja-core`'s is unchanged by this crate's existence. The gate that is the split's whole point is the inverted one: **`ganja-serve` never links this crate.** The closure `ganja serve` ships is the closure a cloud worker will ship, and a worker must provably be unable to spawn a pane on somebody else's machine.

## Key Files

| File | Description |
|------|-------------|
| `Cargo.toml` | Member manifest, `publish = false`. Every dependency carries the reason it is there — `which` (PATH lookup for a pane's binary) and `libc` (unix `killpg` for a shim child's process group) both moved here from `ganja-core` with the backends that needed them. Two `harness = false` `[[test]]` entries (`teammate_pane_lifecycle`, `teammate_pane_env`) moved here too, and a third (`teammate_pane_exit`, **D541**) was registered beside them: each test binary is its own pane child, spawned as `current_exe()` with spawn flags libtest would refuse on sight. |
| `src/lib.rs` | The crate doc — the source of truth for what this crate is and why its `tmux.rs` is not the sealed leaf — and `pub fn backends(shell: PaneShell, share: PaneShare) -> Backends`, the one assembly a shipped session runs through — `ganja-tui`'s `run()` is its only caller. |
| `src/tmux.rs` | The `tmux` calls the two pane backends are built on (P25b). |
| `src/pane.rs` | A teammate in a `ganja` pane of its own (P25b); mints **D502** — an enumerated environment, and a launch line ≥2 words so tmux's own `$SHELL -c` detour cannot re-import credentials the enumeration left out; and **D541** (2026-08-29) — the member watches its own pane on the shared liveness cadence and posts an `Exited` when the pane stops being its own, so a pane a person closes leaves the roster the way a `shutdown_approved` already left it. |
| `src/claude.rs` | A teammate that is a real `claude` pane (P25b); re-exports `teams_root`/`REFUSED_NO_CONFIG_DIR`/`TEAMS_DIR` (as `TEAMS_DIRECTORY`) from `ganja_core::teammate`, where W1 hoisted them because stayers (`lead_inbox.rs`, `engine.rs`) need them whether or not a Claude pane is ever spawned. |
| `src/shim.rs` (+ `src/shim/records.rs`) | A teammate that is another vendor's CLI driven through its own non-interactive door (**D508**, **D509**); the headless `ShimBackend` here is reachable only through the tests that drive it against a fake CLI, since **no spawn door in this build reaches it** (D512). `shim/records.rs` is the per-lead orphan record a shim child leaves behind under `/tmp/ganja-<uid>/`. |
| `src/shim_tui.rs` | The same three CLIs rendered in their own native TUI, in a pane of their own, spoken to through bracketed paste (P28, **D512**) — the door every shim spawn actually reaches, and where the `TuiDriver` companion trait is defined and implemented for all three CLIs. |
| `src/codex.rs` | A teammate that is a headless `codex exec` child, implementing the shared `Driver` trait both the headless shim door (`shim.rs`) and the TUI door (`shim_tui.rs`) dispatch through (**D508**, **D509**). |
| `src/grok.rs` | The same for a headless `grok` child (**D508**, **D509**, **D510**). |
| `src/agy.rs` | The teammate that **ships write-capable, and says so** (**Dv-7**, amending W4's measured no-ship): `--sandbox` bounds the terminal only, with no enforced filesystem bound, and the posture row names that absence rather than hiding it. |
| `src/reaper.rs` | Killing panes the lead left behind when it died (P25b), and sweeping shim orphan records under "no owner proof, no signal" (**D506**). |
| `src/readback.rs` | Carrying a shim pane teammate's answers back to its lead (**D515**): the per-CLI transcript readers, and `answers_clause`, the one clause that says what each CLI carries. |
| `src/liveness.rs` | Whether a teammate's pane is still that teammate's, asked on one cadence for every shape that holds a pane (**D541**): `LIVENESS_POLL`, the `Gone` vocabulary and the listing check both `shim_tui.rs` and `pane.rs` drive — one question and one rule for reading the answer, since two copies of that rule would be two places for it to drift. |

## Architecture

`TeammateBackend` (`ganja_core::teammate`) and `Spawned` are the seam this crate implements — `spawn` yields a `Spawned` that owns launch, the bridge loop, liveness, the recent-calls ring and kill, so nothing outside a backend's own module knows *how* a member of that kind runs. Five external adapters implement it: `GanjaPane`/`ClaudePane` (`pane.rs`/`claude.rs`, a `ganja` or a real `claude` split into a tmux pane) and three `ShimTui` instances — one per foreign CLI (`codex.rs`, `grok.rs`, `agy.rs`) — each spoken to through bracketed paste in its own native TUI. A sixth implementation, the headless `ShimBackend` in `shim.rs`, exists and is fully tested but is reachable by **no spawn door in this build**: since **D512** every shim spawn opens a pane instead, so `ShimBackend` is exercised only by the tests that drive it against a fake CLI, through `ganja_testkit`.

`pub fn backends(shell: PaneShell, share: PaneShare) -> Backends` in `lib.rs` is the one assembly a shipped session runs through, called from `ganja-tui`'s `run()` and nowhere else (`ganja-cli` reaches the same adapters by starting that frontend; `ganja_testkit::externals()` deliberately assembles its own headless set instead): it builds the five external adapters and returns them for the caller to hand to `Engine::with_teammates`, which inserts its own `in-process` sixth entry — the one adapter an engine can build entirely out of its own provider, tool set and store, and the only `MemberBackend` this crate does not implement. A backend absent from an assembled map is refused by name at spawn rather than silently downgraded to `in-process` — `Backends::of` returns `Option`, and both call sites in `ganja-core`'s `subagent.rs` turn a miss into that refusal.

**Pane layout and orphaned-pane reaping are this crate's own narrative, not `ganja-core`'s** (**D502**, **D506**, from P25's landing): Panes are laid out as one column beside the lead, filling downwards — the first teammate splits the lead `-h` at 65%, leaving it 35% (2026-08-25; 70/30 from 2026-08-20 until then) — or at whatever `teammates.pane_share` names — and every later one splits the bottom of that column `-v`; which of the two a spawn gets is read off tmux's own geometry (`Server::column_bottom`) rather than remembered, because a teammate's pane may hold a foreign CLI that no `ganja` argv predicate would match and because a person may have closed one (user directive, 2026-08-20). A pane inherits an **enumerated** environment rather than the parent's (**D502**), and its launch line is ≥2 words on purpose: tmux runs a one-word command through `$SHELL -c`, which sources rc files and re-imports exactly the credentials the enumeration left out. `reaper.rs` ends an orphaned pane only on what it is **running** — argv carrying both `--agent-id <id>` and this lead's `--parent-session-id` — never on what was recorded, because a pane id is recycled (**D506**).

**D541** (2026-08-29, bead `ganja-code-okip`, the first decision minted here after the split rather than moved in by it): a pane teammate watches its own pane and reports its exit; the lead retires it on the existing `Exited` path; the reaper stays a cold-start sweep. Until then `PaneMember::alive()` answered `true` forever, `start` handed the registry no task and both pane backends threw their `Lent` away, so a pane killed out from under the lead left the member on `/team` for the rest of the session and a `/team shutdown` aimed at it only queued a request nobody was left to read — `reaper::sweep()` is a *previous* lead's orphans (**D506**), never a poll of this one's. The cure is the poll `shim_tui.rs` already ran, hoisted to `liveness.rs` so there is one cadence and one rule for reading a listing, and an `Exited` posted the first time the pane is no longer this member's — carried through the registry the way a shim's already is, and retired by `TeammateRegistry::take_exited` → `retire_exited`, the door a `shutdown_approved` takes, so a pane a person closes leaves the roster and the team file the same way. Three properties are the decision rather than the implementation: the poll **only reports** and never kills a *live* pane by id, because `%N` recycles — a corpse it finds goes through the dead-only door, where tmux itself decides deadness and the kill in one command; the fate it reports (`PaneFate`: closed, left, or recycled) is read off tmux — a recycled id off the listing, everything else through that door — rather than assumed; and `alive()` is a cached flag rather than a listing, because the status bar's teammate count polls it every tick (D503) and the `/team` roster reads it on every open. A deliberate `kill()` cancels the watch before it kills, and the watch checks that token again after every listing before it reports, so a member the lead retired on purpose is never reported a second time as having died. `Exited.cli` became `Option<ShimCli>` for this (Dv-2 of the run that landed it): a `ganja` or `claude` pane runs no CLI this build shims for, and the retirement builds `Surface::Pane` from `None` and `Surface::Shim` from `Some`, so a shim member's `Exited::notice()` did not move a byte. The tests' hygiene came with it and is that run's Dv-3 (both recorded in `.omc/handoffs/team-plan-followups.md`): `ganja_testkit`'s `PrivateServer` already kills the server on every road out that unwinds, but a signal-killed test process runs no destructor, and such an orphan was immortal — a first window sleeping an hour beside a pane child sleeping forever, and tmux exits only when its last pane does — so `pane_support` bounds both at five minutes (`CHILD_LIFE`, `IDLE_WINDOW`), past any run of these binaries and past nextest's own four-minute terminate-after, and the orphan empties itself; the shim stubs' `cat` panes are the residual, bead `ganja-code-z471`.

- `shim.rs` (with `shim/records.rs`) + `codex.rs`, `agy.rs`, `grok.rs` — since **D539** all here — P27's landing: a teammate that is somebody else's CLI. Neither upstream opencode nor Claude Code runs another vendor's agent as a teammate, so every sentence here is ganja's own; and none of it is a fourth provider — a shim teammate runs the foreign CLI's own agent, its tools and its sandbox, and this side only carries words in and words out. **Three named shim backends over one two-shape core** (**D507**) — codex and grok as `Shape::PerMessage`, one child per inbox message; `Shape::Resident`, one child for the member's whole life fed a line of NDJSON per turn, is the shape agy is driven on. A per-CLI module answers only what that CLI spells differently. The core's loop mirrors `runner.rs`'s and adds one guard it does not have, because of what the two deliver into: an in-process teammate reading odd JSON is a model reading odd JSON, while a shim pasting the same text into a foreign agent's prompt is ganja's internals leaving the building — so **any inbox message whose text parses as a JSON object bearing a `type` key is not prompt material**, total by construction because `Frame` is `#[serde(tag = "type")]`, and implemented on a predicate that can tell "no `type` key" from "a kind this build has never heard of", which `reserved_kind` cannot. The runtime holds such a member as a `shim::ShimMember` (Dv-5(f); the old `Handle::Child` variant left with **D538**), and the record it writes into the teams directory is a `Surface::Shim { cli }`: a per-CLI `backendType`, beside the **in-process sentinel** in `tmuxPaneId` — a lie an older ganja and a real `claude` both survive, where a fabricated pane id would aim pane machinery at a process that has none, at the cost of one lossy direction on the read back (a pane-mode shim writes the **real** pane id there instead, D512, lossy the other way and for the same safety reason). **What a person consents to** (**D508**): v1 grants a shim teammate **read, and not write** — except agy, whose floor **Dv-7** extended to **write-capable** on a user directive, at the honest posture enumerated below. There is no escalation tier at all — until 2026-08-22 a `SpawnSpec` carrying `bypass` was refused by name rather than quietly downgraded, and since **D513** retired the bypass axis there is no such spec to refuse — the posture is pinned on **every** turn including resume (two `-c` keys for codex, because `codex exec resume` carries no `-s` and this machine's own config reads `danger-full-access`; one axis for grok, whose `--sandbox read-only` is the bound while the `--permission-mode dontAsk` beside it is labelled with what it measurably does rather than credited as an approval axis it never reaches), and every shim spawn raises `Ask` on its own always-asking `teammate_foreign` permission, which no stored allow can silence. The grant is described by **what it bounds**, and where the floor is large by what it **enables**: codex's row reads "writes denied, whole-disk read, network denied — may read any file you can, including credentials, but has no network to send them over", grok's "writes denied outside `~/.grok` and temp, whole-disk read, no network bound (macOS) — may read any file you can, including credentials, and may send them anywhere; reading takes no approval, and a tool request that needs one ends the turn", and agy's — the only row describing the **absence** of a bound, and therefore the clause's own case — "sandbox: terminal bounded, no enforced filesystem bound — may read any file you can, including credentials, and write anywhere you can; those writes are outside the snapshot chain /undo walks". Two rules fall out of that shape, and in P27 both bit: **a floor nobody can state in a priceable sentence does not ship**, and **no posture row, dialog sentence or ring line ships ahead of that CLI's own gating probe**. **What a child is, and what ends it** (**D509**): headless child processes, never panes and never `send-keys` — the sealed `tmux` crate is untouched — (**amended for the shim trio by D512**, the next bullet: since P28 every shim spawn door opens the CLI's own TUI in a pane, and the headless door stays in the tree reachable by the testkit alone) and a **headless** shim is the only teammate shape whose *turn* is bounded, deliberately, because it is the only one whose progress ganja cannot observe: an in-process teammate streams events into this process and a `ganja` pane is a ganja whose status bar a person can look at, while a shim child that has stopped writing to a pipe is indistinguishable from one that is thinking. One mechanism bounds both shapes, the *value* is per CLI — codex's and grok's derived from their own probes' wall-clocks, agy's from an *ordering* against its own `--print-timeout`, which the driver then re-derives from the effective deadline so one config key moves both — and one curated `teammates.shim_turn_timeout` moves all three headless drivers so the number is never a source edit — a key that since D512 governs that headless machinery alone, no spawn door reaching it. Orphans are handled without trusting what a record claims: own process group plus `kill_on_drop`, stdin EOF as the first line, and a `/tmp/ganja-<uid>/<stem>-<lead pid>.shims` record whose first lines carry a version token and the recording lead's own `(pid, start-time)`, written `tmp`+`rename(2)` and compared through a `ps` pinned to `TZ=UTC`/`LC_ALL=C` so an identity token cannot drift under a live process; the sweep is governed by **"no owner proof, no signal"** — a file whose owner is live is left entirely alone, and an owner that cannot be proven gone is a log line rather than a branch — and it runs where `reaper::sweep` does not, being ungated on a reachable tmux server. `socket::prepare_directory` moved down into `ganja-tool` for it, **amending D505's split**. **How each CLI is spoken to** (**D510**): codex is `codex exec` then `codex exec resume`, prompt on stdin, every `AgentMessage` item in the JSONL forwarded as its own mail in order; grok is one child per message reading the Messages-NDJSON output — a shape this repository already speaks fluently, where that vendor's ACP output would be a new protocol surface adopted for one CLI — with a v7 UUID minted on the first turn's line, so `--session-id` can never title-match somebody else's conversation, and that id echoed back off the child's own `init` record for the resume, and the prompt in a `0600` file whose *path* the argv names, because argv is world-readable through `ps`. agy's resident stream-json wire is `{"event":"user","message":{"content":"…"}}` in and `init`/`step_update`/`result` out — keyed on `event` rather than `type`, with the terminal `result` the only record carrying any text, so a lead hears from an agy teammate once per turn. **What measurement changed** (**D511**): the plan was written before any probe ran, so six deviations are recorded in its own ledger rather than quietly absorbed — the `pane` backend renamed `ganja` and made the default (Dv-1); `Surface::Foreign`/`ForeignCli` renamed `Shim`/`ShimCli`, because a `claude` binary is foreign too and what actually distinguishes these three is that they do not speak the mailbox (Dv-2); two resume-story clauses ruled narrower than they read, since under v1's name rules a retired member's name is never reused and so the identity a "context lost" mail would report on cannot exist (Dv-3); one crash-stranded staging file accepted as residue rather than raced away from a concurrently live lead (Dv-4); codex's `-c`-versus-profile precedence question found *unbuildable* rather than unbuilt, a config-selected profile being refused at load by the probed version (Dv-5); and agy's prescribed launch line found unparseable, `-p` taking the prompt as its flag value (Dv-6). Three of the verdicts are the probes' and not the plan's — **codex ships**; **agy did not, and then did**: its `--sandbox` bounds the terminal only, and in the same runs where a shell write outside the working directory was denied, agy's own `write_to_file` tool wrote to an absolute path outside it in 2 of 2 runs of that flag set — measured grounds for a no-ship, which a user directive then reversed (**Dv-7**), shipping it as a **write-capable** teammate whose posture row says exactly that and adds the clause a reader could not derive, that those writes are outside the snapshot chain `/undo` walks; and **grok ships as wedges-with-mail**, because a tool request that needs approval ends its turn, which its row, its dialog and its ring say rather than hide — a label sized to 1.0.6, where every tool ask wedged; at 1.0.7 only a shell write the sandbox would deny still asks, so the wedge is rarer and the label stays because the mechanism it names did not change (the re-probe below). Each shipped sentence is compared against that CLI's own recording under `crates/ganja-teammate-local/tests/fixtures/*-posture-probe.txt` rather than against a second string literal, because two literals agreeing proves only that somebody typed carefully. What P27 left open is carried as beads rather than as prose, and one of them was decided twice: this machine's own `~/.grok` **was** a symlink, and that vendor's read-only profile refuses to start under one, so every grok turn **on this machine** refused with the vendor's own sentence as structured mail — an operational fact about one laptop and not a statement about grok's code (the backend ships, and its refusal arriving as mail is the failure channel working) — which the user first settled by scheduling `GROK_HOME` as a credential-home addition on the `CODEX_HOME` precedent, a D508 amendment deliberately outside P27's scope (`ganja-code-q98`), and then, on 2026-08-20, un-symlinked the directory; grok 1.0.7 ran real turns under the same launch line that day, the live walkthrough P27 deferred was walked under P28 (the D512 bullet and that plan's ruling 16), and the bead now carries only the carve-out a machine that keeps its symlink would still need. codex is the backend P27's own acceptance was walked on. The same re-probe closed one of the behavioral beads: `ganja-code-vaz` (grok's network bound shipped unmeasured, its sentence deliberately over-disclosing) is measured at 1.0.7 — a shell `curl` reaches a public host unasked, headless and in the TUI alike, so "may send them anywhere" is literal, and the live ladder's arm (d) pins it. It also recorded a drift the bound sentence survives: at 1.0.7 a sandbox-*permitted* shell command or write runs unasked where 1.0.6 asked and cancelled it, and only a shell write the sandbox would deny still asks — every bound clause holds, "a tool request that needs one ends the turn" is still what happens to the asks that remain, and what shrank is the **set** of requests that ask (`grok-posture-probe.txt`'s re-probe block; the ladder asserts 1.0.7's answers, and a 1.0.6 binary fails it by design). The other seven stay ordinary follow-ups: the acceptance tail is carried as `ganja-code-qq5`, `ganja-code-zte`, `ganja-code-lp1` and `ganja-code-1o9`, and the behavioral tail as `ganja-code-v69` (a grok child that prints its session id and then dies loses it), and `ganja-code-0tv` and `ganja-code-98v`, the review tails W2 and W3 each left.
- `shim_tui.rs` + the pane vocabulary in `tmux.rs` — since **D539** both here — P28's landing (**D512**, `.omc/plans/2026-08-20-shim-native-tui-panes.md`, user directive 2026-08-20): a shim teammate is its CLI's **own native TUI in a tmux pane** beside the lead, spoken to through bracketed paste — not a log viewer and not the headless child. Neither upstream opencode nor Claude Code has a counterpart, so every sentence is ganja's own; and it **amends D509's "never panes and never `send-keys`"** for exactly these three backends while leaving the sealed `tmux` workspace crate untouched (every tmux word here is `ganja-teammate-local`'s own control client). `ganja_teammate_local::backends` wires all three shim slots to one `ShimTui` over a `TuiDriver` companion trait defined and implemented in `shim_tui.rs` over the sibling drivers (the lead's ruling 3: a local trait over sibling types reopens no driver file and adds no second `match MemberBackend`), each answering `tui_argv()` — **the pinned D508 floors and nothing else**, never a prompt and never an identity flag: codex `-c sandbox_mode="read-only" -c approval_policy="never"`, grok `--sandbox read-only --permission-mode dontAsk`, agy `--sandbox` — and a `READY_MARKER`, both pinned against that CLI's own recording under `tests/fixtures/*-tui-probe.txt` (codex's composer line `› Ask Codex to do anything`, agy's footer `? for shortcuts`; grok's its composer glyph `❯`, since that composer draws no placeholder — pinned on a second recording taken after the user un-symlinked this machine's `~/.grok`: under the symlink grok 1.0.6 refused its sandbox profile with the headless sentence byte for byte, bead `ganja-code-q98`, and that refusal stays in the fixture as the dead-pane case). **Spawn**: no reachable tmux server refuses **by name**, exactly as the `ganja` backend does — no headless fallback, no config door; the D508 always-ask `teammate_foreign` gate is untouched; the pane is split through the same `split_idle_shell` the two native pane backends share and placed by `Server::column_bottom`; `remain-on-exit` is set **before** the launch line so a CLI that dies leaves its refusal readable; the launch line is `exec <binary> <tui_argv> || exit` through the pinned `launch_line`/`type_line` pair (shlex single-quotes codex's `-c` values, the pane shell strips them, codex reads the TOML bytes exactly — pinned by a round-trip test; the `|| exit` tail, since 2026-08-25, ends a shell whose exec came back so it is refused by its own last words on the death road below rather than left prompting under the readiness poll); and the pane's environment is `CARRIED_ENV` plus the driver's admitted `additions()` (`CODEX_HOME` today), composed through the same `shim::admits` class rule as the headless enumeration — but a pane **inherits the tmux server's environment and `-e` only adds**, so no pane-mode sentence claims the environment is enumerated (D502's claim does not carry). **Readiness is a poll with a ceiling, never a gate**: `capture-pane -J` every 250 ms for 15 s looking for the marker — **below the launch line's own row**, since 2026-08-25: the pane's shell is the person's own (D520) and its prompt may draw the composer's very glyph (grok's `❯` is every popular zsh theme's; a marker the shell drew passed for the composer that day, and the paste that followed landed in a grok still drawing, which dropped the Enter), and on a screen the launch row is gone from only once `#{pane_current_command}` no longer answers what it did before the line was typed, which is what tells a CLI's cleared screen from a prompt drawn before the echo (`composer_shown`); seeing it is the ordinary case — **held one second** (`READY_SETTLE`) before the first paste, because a composer that has just drawn is not yet one that submits: the W5 walkthrough's first live spawn pasted and pressed Enter 0.2 s after codex's composer line showed and the text sat unsubmitted while the ring said "delivered" (4 of 5 runs at 0.2 s; 6 of 6 submitted at 0.5 s, recorded in `codex-tui-probe.txt` and pinned) — and is then re-checked against a live `panes()` listing so a marker printed on a corpse cannot yield a live member (HIGH-2), nor one that died during the settle; not seeing it is a ring note and a proceed, because a first spawn in an untrusted directory shows a trust dialog and a logged-out CLI its login screen, both answered by a person in the pane — codex's headless `ready()` login pre-check is deliberately not run for a TUI spawn for that reason — and what the timeout changes is **whether Enter is pressed**: a spawn that saw its marker pastes and submits, one that timed out pastes and presses nothing, since an Enter into a pane that may be holding that dialog answers it unseen (HIGH-3; the text waits in the composer for the person). The one failure is the pane's process **dying inside the window** — grok's every spawn under the symlinked-home recording — refused as "the {cli} backend is unavailable: {cli} exited before its composer was ready; its pane said: <vendor's last line>", the last words captured *first* and only then the dead pane closed through `close_dead`, because a dead pane left on screen halves the teammate column on every refusal (ruling 8c; `column_bottom` counts dead panes deliberately). **Delivery**: one inbox message is `load-buffer -b <name> -` with the text on the client's **stdin** (never argv — `ps` is world-readable, the rule grok's prompt file already encodes) then `paste-buffer -p -d -b <name>` **in one client invocation** so the buffer's whole life is one process and a delivery dropped mid-await leaves no cleartext buffer behind (F1, measured: tmux sets a buffer only from a read it saw end), then `send-keys Enter`; the stub receives exactly `ESC[200~ body ESC[201~ \n`, pinned byte for byte. The body is **control-neutralized before it is framed** (`paste_body`): the paste's own close marker is a control sequence, and a peer's message containing one would otherwise end the paste early and land what followed as raw keystrokes in the foreign composer (measured live) — so every control character but `\n` and `\t` is stripped, the invariant `ganja-tui`'s notifier already holds for text inside an escape, narrowed to keep the two a composer reads as content. Deliveries are **serialized per member** (`paste_submit` is not safe against concurrent calls to one pane), retired at write time under `Delivery::FireAndForget` (D503), and a failed delivery is a ring note **and a word back to the sender**, never a blind redelivery — the text may be sitting pasted-but-unsubmitted (ruling 8a/b, MEDIUM-5). **No per-turn deadline in pane mode**, by D509's own rationale: the deadline existed because a headless shim's progress was unobservable, and a TUI in a pane is a thing a person can look at. **v1 was send-only**: the lead's words reached the composer and the lead heard nothing back — until **D515** (2026-08-24, user directive; `readback.rs`) closed bead `ganja-code-9u1` by carrying a pane teammate's answers back out of **its own CLI's transcript** (codex's rollout JSONL, grok's session `updates.jsonl`, agy's `transcript.jsonl`), found by fingerprint — the preamble's own opening sentence, which names one member of one team, so a pane is never read as another's — polled every two seconds beside the liveness poll and mailed as this member, which reaches the lead as the same `PartBody::Peer` any teammate's words do. What each CLI carries is its own driver's contract, stated once in `readback::answers_clause` and told to headless children and panes alike: every message in order for codex, the turn's final answer for grok and agy. So the sentence a person reads is now the opposite of D512's, in the same position and for the same reason — `HEARD_BACK` ("its answers are mailed back to you"), not `SEND_ONLY` — and the pane preamble says what will be carried rather than that nothing will be (codex's own `codex queue` injection door remains `ganja-code-tvh`); every sentence a person reads says so: the spawn dialog carries `posture` (D508's bound, unchanged and still pinned to `*-posture-probe.txt`) **and** `surface`, the backend's own `surface_line()` — `pane_line`, per CLI, **opening** on one `HEARD_BACK` clause (`its answers are mailed back to you`, D515's replacement for D512's `send-only: the lead hears nothing back`) — and the `/team` ring opens on `shim::posture_lines` and closes on that same string (`shim_tui::spawn_lines`), not on the headless door's grok `--permission-mode` rider (ruling 5). The clause comes first and the whole row is short because of where it is read: the spawn dialog is the generic permission modal, whose clamp leaves a seventh argument about two rows at its widest, and a ring row is one line cut at the dialog's width — `ganja-tui`'s permission test renders the real sentences at 80x24 and asserts the clause survives for all three (the review of W4 found the first draft's tail-position clause cut off on every backend). Per CLI because the pane-mode fact differs under each CLI's flags, and each says only what its `*-tui-probe.txt` recorded: codex's that under `approval_policy=never` it asks no approval (a denied tool is denied); agy's that it opens in **accept-edits mode: file edits auto-approved** (the probe's own `outcome:` words) beside the Dv-7 write-capable bound; grok's is the row that has to contradict its own bound on purpose: the headless measurement behind `posture_line`'s "a tool request that needs one ends the turn" does not carry to the TUI, where (measured on 1.0.7) the write tool is blocked outright, a proposed shell command raises grok's **own** three-option approval prompt to the person — `dontAsk` silences nothing there — an approved write into the working directory is still denied by `read-only`, and a rejection is what ends the turn; so its row says that grok asks the person in the pane before a tool that needs approval, that the bound holds against their yes, and that only their no ends the turn — and a reader of the spawn dialog sees only the first of those three, which is why it is the one row whose shape is inverted: the spawn dialog is 76 columns at its widest and grok's four-row bound sentence leaves this row exactly one line there, about twenty characters past the read-back opener, so the ask comes first and the "its own TUI in a tmux pane beside you" preamble is pushed behind the facts with the flag's name last; the ask is the fact a person has to act on (a pane waiting for them), so it is the twenty characters that survive, `ganja-tui`'s 80x24 render test asserts they do, and the `/team` ring's wider row carries the bound-holds clause after it while the rest of the sentence is the string and not the screen. Nothing claims a trust dialog or a login screen renders: neither is in a recording. **Record**: `Surface::Shim { cli, pane }` writes the per-CLI `backendType` beside the **real** `%N` in `tmuxPaneId`, so the record reads back as `Surface::Pane` — lossy the other way from the headless sentinel and safe for the mirror reason (the pane exists) — and `reaper::sweep_on` recognizes a shim pane **by `backendType` before the surface match** and leaves its record alone (MEDIUM-4): a foreign CLI's argv carries no `--agent-id`/`--parent-session-id` pair, so the D506 witness cannot match it, and without that recognition a same-session restart read it as `Recycled` and dropped this teammate's own record while correctly leaving the pane (full witnessing is `ganja-code-3nz`; a `ganja shim-host` wrapper restoring reapability is `ganja-code-tss`). A lead that dies leaves its panes, correctly, under "no owner proof, no signal" — unlike a headless child the orphan is a visible TUI a person closes by hand — and no `.shims` orphan record is written for a pane, because that sweep signals only what ganja forked. **A pane that stops running after readiness** — the CLI quit, crashed, or a person closed the pane — is noticed by the member's own loop rather than by the next paste that would have failed into it (`LIVENESS_POLL`, two seconds, four inbox passes; a listing that *fails* proves nothing and retires nobody, the reaper's own rule): the pane's last line is read while the corpse is still on screen (and only from a pane still this member's — an id recycled under another pid is somebody else's screen, never quoted), the messages left in that member's inbox are answered to their senders rather than left unread, the corpse is closed through the same dead-only door with no shutdown asked — and what `end` actually left is what every sentence then says (`PaneFate`: closed; a dead pane tmux would not take away, named as a person's to close; a recycled id, left alone), never assumed — the lead's model is told in prose, and the registry is handed an `Exited` the lead's next §6.2 pass retires the member on — `TeammateRegistry::retire`, the door a `shutdown_approved` takes, so the member leaves the roster and the team file the same way — reported under `Pass::exited` so a frontend says it once (`Exited::notice`, on the status bar or the `/team` dialog's notice line). Carried through the registry rather than as a frame because no frame says it honestly (a `shutdown_approved` answers a request nobody sent; `teammate_terminated` is the lead's word to a teammate). Measured: `paste-buffer` into a dead pane is refused (`target pane has exited`), so a delivery that races the exit takes the failed-delivery road and tells its sender, while one that landed in a composer still drawing is lost in the pty, honestly, under `FireAndForget` (bead `ganja-code-g9u`, landed 2026-08-20 on a user directive that chose retire-and-close over leaving the corpse). **Shutdown** is ruling F3's order: `SIGTERM` to the pane's process group **first, while the pane still pins the pid** and the recorded `(pane_id, birth)` pair can vouch for it (agy survives the `SIGHUP` a bare `kill-pane` sends — measured), wait for the pane to stop listing live, `SIGKILL` if it did not, and only then `close_dead` what remains; signalling after the kill would signal a pid the kernel may have handed to somebody else. **Ruled mid-flight and recorded in the plan's own ledger** (rulings 1–12): `capture` adds `-J`; named per-call buffers because the unnamed stack is server-wide and D462's fan-out makes concurrent deliveries real; `Server::panes()` skips dead panes (a remain-on-exit corpse lists with an empty `#{pane_pid}`) and deadness is `#{pane_dead}`'s answer, never an absent pid's; `Closed::Refused` exists so a dead pane whose kill did not take is not reported alive; and a `paste`-only door exists beside `paste_submit` for the timed-out case. A silent-composer spawn holds the `/team spawn` door for the 15 s readiness ceiling, by design (the door is the team file's single writer; a second spawn meanwhile reads `BUSY`). What P28 left open rides as beads: `ganja-code-0zd` (`gone_within`'s 50 ms poll cadence), beside the four above; `ganja-code-g9u` (a TUI that dies *after* readiness) landed the same day, above.

## For AI Agents

### Working In This Directory

- **This crate may name `ganja-core`; `ganja-core` must never name this crate.** The direction is the compiler's rule (CI's closed allowlist), not a convention a reviewer remembers — the same shape `ganja-tool`'s and `ganja-provider`'s own splits use.
- **Never link the sealed-leaf `tmux` workspace crate.** This crate's own `tmux.rs` is a from-scratch control-mode driver, not that crate; CI's `tmux consumes no ganja crate` / `no workspace member consumes tmux` pair proves the two stay unrelated in both directions, and the derived-member loop covers this crate with no edit.
- **`ganja-serve` must never be able to reach this crate**, even transitively — that is the split's whole reason to exist. An edge that would put this crate in `ganja-serve`'s normal tree is the one change that undoes it; CI's inverted gate catches it, but the design intent is to never need the catch.
- **A pane-mode shim has no per-turn deadline.** `teammates.shim_turn_timeout` governs only the headless `ShimBackend` this crate also holds, which no spawn door here reaches — do not wire it to `shim_tui.rs`'s panes.
- **A backend that cannot spawn refuses by name.** `backends()` never falls back silently; an absent or unassembled backend is a `Backends::of` miss the caller turns into a named refusal, never a downgrade to `in-process`.

### Testing Requirements

```sh
cargo nextest run -p ganja-teammate-local     # unit + integration, each test its own process
cargo test -p ganja-teammate-local --doc      # doctests
```

Nineteen `tests/` binaries carried over by the seam split — and a twentieth born after it, `teammate_pane_exit.rs`, **D541**'s integration proof — most needing either a reachable tmux server (`pane_support/mod.rs`) or a fake-CLI harness (`shim_support/mod.rs`); `teammate_no_tmux.rs`/`shim_tui_no_tmux.rs` are the pair that must pass on a machine with **no** tmux at all — the refusal-by-name path. Two groups sit outside that, and for opposite reasons. `teammate_pane_lifecycle.rs`, `teammate_pane_env.rs` and `teammate_pane_exit.rs` are `harness = false` — each is its own `fn main()`, because the program `pane.rs` starts in a teammate's pane is `current_exe()` and libtest would refuse the spawn flags on sight — so `#[ignore]` is not a thing they can express: they need a reachable **tmux** and **hard-fail** without one, on the golden differential's posture that a run which tested nothing must not look green. The `*_live.rs` binaries are the other way round: `#[ignore]`d, inert unless `GANJA_LIVE_TEST=1` is set, and needing the real vendor CLI (`claude`, `codex`, `grok`, `agy`) on `PATH` — opt-in the same way `ganja-core`'s own live provider tests are. See the crate's own `tests/AGENTS.md`.

### Common Patterns

Every dependency is `x.workspace = true`; versions live in the root manifest with their rationale. `#[cfg(test)] #[path = "..._tests.rs"] mod tests;` is the sibling-file pattern the whole workspace uses (see the root `AGENTS.md`'s Testing Requirements); anything needing a real socket, a real tmux server, or process-wide state lives in `tests/` instead.

## Dependencies

### Internal

Exactly `ganja-core ganja-permission ganja-protocol ganja-provider ganja-storage ganja-team ganja-tool `, the string CI's allowlist asserts against this crate's `-e normal` tree. Four are named in the manifest: `ganja-core` (the engine this crate is an adapter for: `TeammateBackend`, `Spawned`, `Lent`, `SpawnSpec` and the `Backends` map `backends()` fills), `ganja-protocol` (`MemberBackend` and the team frames a preamble and a delivery are spelled in, named directly rather than through the engine's re-export), `ganja-team` (the teams directory format a spawn's record and a delivery's mailbox both write into), and `ganja-tool` — for `socket` alone: the session-socket directory scheme (**D505**/**D509**) a headless shim's orphan records are kept under, and the `vet_directory`/`prepare_directory` pair a sweep runs before it walks one. The remaining three arrive through `ganja-core` and are named nowhere here: `ganja-permission`, which the engine sits on, `ganja-provider`, whose Messages decoder `grok.rs`'s module doc cites as the thing it deliberately does *not* reuse — an intra-doc link through the engine's own `ganja_core::provider` facade, not a dependency of this crate — and `ganja-storage` (**D540**, W3), which this crate reaches only as whatever the engine it drives happens to persist through.

### External

`async-trait` (every backend is `Arc<dyn TeammateBackend>`, dispatched by name at spawn), `shlex` (a pane's launch line is a shell command line, and the config-named shell it splits into arrives already split — the one lexer both halves agree on with tmux's own), `tempfile` (a shim child's records and a pane's staged files are written through a temporary and renamed, the same rule the team file follows against the same reader), `which` (a pane-backed teammate resolves a binary this process may execute by asking the operating system rather than reading mode bits — moved here with the backends; nothing left in `ganja-core` looks a binary up), `libc` (unix only — a shim child is ended by signalling its process group), `serde`/`serde_json`, `thiserror`, `tokio` + `tokio-util` (`rt`, for `CancellationToken` — a backend's own token is a child of the registry's), `tracing`.

<!-- MANUAL: -->
